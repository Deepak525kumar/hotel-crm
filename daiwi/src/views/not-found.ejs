<!doctype html>
<html lang="en">
<head><title>Not found</title><%- include("partials/head") %></head>
<body class="auth-page">
  <div class="auth-card">
    <h1>Not found</h1>
    <p class="muted">This link has expired, been deleted, or never existed.</p>
  </div>
</body>
</html>

node_modules/
dist/
storage/
uploads/
*.db
*.db-journal

# Generated by scripts/use-env.sh — the running configuration, with secrets.
.env
.env.backup.*
# Production secrets, created by hand on the host and never committed.
.env.production.local

# Artifacts written by test/e2e.sh during a run (cookie jars hold live session
# tokens; the downloads are just copies of the fixture).
test/e2e-cookies*.txt
test/e2e-dl.ipa
test/fake.ipa

# Module Specification: `backend-hr`

## Document Control

| Field | Value |
|---|---|
| Spec ID / version | `SPEC-HR-001 / 0.2.9` |
| Status | `REVIEW` (post-re-review correction; **NOT FROZEN** — `OD-HR-01a`/`OD-HR-01b`'s module-boundary block is **RESOLVED**: `ADR-012` (Accepted, 2026-07-12) already settled this exact dispute — `backend-hr`/`SPEC-HR-001` is the ratified canonical owner of contract generation and manager-confirmation capture; `docs/03-modules/onboarding/MODULE_SPEC.md` is a consumer only. This document's own architecture-`BLOCKED` framing and its subsequent byproduct-correction rows were never updated to reflect that 2026-07-12 resolution — corrected here, 2026-07-28, as a documentation-synchronization fix, not a new governance decision. Consistency and security re-reviews previously returned `PASS_WITH_ACTIONS`; see Review and Change Log. G2 freeze remains reserved human authority and is not granted by this correction.) |
| Owner | `unassigned` — reserved human authority (SYNC-001); no `CODEOWNERS` exists and `backend/package.json` author is empty |
| Authors / reviewers | Author: Module Author (documentation workflow). Independent reviewers (G4, on v0.1.0 rev `7a062d2`): Architecture = `BLOCKED`; Dependency = `PASS_WITH_ACTIONS`; Consistency = `FAIL`; Security = `FAIL`; Performance = `PASS_WITH_ACTIONS`. This v0.2.0 applies all author-fixable findings from that round; it does not resolve the escalation-only findings (see `OD-HR-01a`, `OD-HR-01b`, `OD-HR-15`) and remains open to a further review pass. |
| Repository revision | `b52f9cc75c413a8bb1eb4f87803be009ea5766eb` (`b52f9cc`) for v0.1.0 facts; re-verified against current worktree for this correction pass (`backend/src/modules/hr/routes.ts`, `backend/src/modules/hr/controller.ts`, `backend/src/middleware/permissions.ts`, `docs/03-modules/onboarding/MODULE_SPEC.md`) |
| Approved by / at | — (G2 freeze requires the named human approver; not yet approved; freeze additionally blocked by open architecture/consistency/security findings) |
| Supersedes | None — no prior specification exists at this path (this is a version bump of the same spec, not a new document) |

> **Authoring note (not a normative section change).** Behaviour is derived **exclusively** from the two authoritative documents — `docs/00-foundations/CONFIRMED_REQUIREMENTS_REGISTER.md` (**CRR §n**) and `docs/00-foundations/PIVOT_DESIGN_DOCUMENT.md` (**PDD §n**) — plus the current worktree for repository facts. No behaviour is drawn from marketplace-era or `docs/legacy/**` sources. This spec documents the physical code module `backend/src/modules/hr` (registry id `backend-hr`), which currently implements the combined contracts-lifecycle, payroll/payslip-request, and contract-scan document-upload surface as a single Express router/service. See `OD-HR-01a`/`OD-HR-01b` for the docs↔docs module-boundary ambiguity this creates against `docs/03-modules/employee-management/MODULE_SPEC.md` and the near-empty `docs/03-modules/contracts/`, `docs/03-modules/payslips/`, `docs/03-modules/documents/` directories.
>
> **v0.2.0 correction note.** This version applies G4 review corrections to v0.1.0 (rev `7a062d2`): architecture `BLOCKED`, dependency `PASS_WITH_ACTIONS`, consistency `FAIL`, security `FAIL`, performance `PASS_WITH_ACTIONS`. Critically, v0.1.0 never cited `docs/03-modules/onboarding/MODULE_SPEC.md`, an actively-maintained specification whose §5 Responsibilities (items 4-5) and §6.5/§7 (Onboarding-Lifecycle steps 7-8) assign contract generation and manager-confirmation capture to the **Onboarding** module in first-person ownership language — directly conflicting with this spec's `RULE-HR-01`/`REQ-HR-001` and `RULE-HR-03`/`REQ-HR-003`. This conflict was disclosed, not resolved, in `OD-HR-01a`. **Correction (2026-07-28):** this conflict was in fact resolved by `ADR-012` (Accepted, 2026-07-12) — `backend-hr` is the ratified owner; Onboarding's first-person language is superseded, not authoritative. This note and `OD-HR-01a`/`OD-HR-01b` were never updated to reflect that resolution until now, a documentation-synchronization gap, not a live conflict. All original corrections are itemized in the Review and Change Log (`0.2.0` row). Status remains `REVIEW`; this document is **not** FROZEN (G2 freeze is a separate, still-outstanding step).

## Purpose and Scope

**Outcome:** Generate and hold the legally required handwritten-signature employment contract for each employee, track its lifecycle and expiry (fixed-term → extension → permanent), gate account activation on manager-confirmed signed-contract receipt, and handle the payslip-request-to-manager-email flow — with zero in-system payroll computation (CRR §9, §23; PDD §7.7).

**In scope:**

- Contract generation: producing a pre-filled contract PDF from Personalfragebogen-sourced worker data (name, start date, terms, 1-year fixed term, 6-month probation clause) and making it available to download/print (CRR §9; PDD §7.7).
- Contract status lifecycle: pending (awaiting signature) → signed/active, and the visible "contract pending signature" safeguard status (CRR §9).
- Storing the scanned/photographed hand-signed contract in S3 (EU) once uploaded (CRR §9; PDD §7.7, §9.3).
- The manager-confirmation mechanism (Option A): recording that a manager uploaded the scan and marked it "signed contract received & valid," which is the trusted trigger — the system does not detect, verify, or auto-validate the handwritten signature (CRR §9; PDD §7.7).
- The activation gate: the worker's account cannot activate until a contract file is uploaded **and** marked signed by a manager; this module owns emitting/holding that gating fact for Onboarding/Employee-Management to consume (CRR §9).
- The 1-year expiry clock, its start (on signed/active), the optional +1-year extension (no new probation), and the permanent (open-ended) contract after 2 years (CRR §9).
- Expiry reminders to the responsible manager before the 1-year mark and again before the 2-year mark; no reminders once permanent (CRR §9; PDD §5.6, §7.7).
- Payslip request flow: a worker requests a payslip; the request is recorded and routed for a manager to fulfil by emailing the payslip manually; the worker cannot retrieve the payslip through the system (CRR §23; PDD §7.7).
- The document-upload endpoint (`POST /workers/:worker_id/documents`) exposed by this module, in scope **only** as the mechanical upload mechanism the contract-scan flow uses ("mechanically treated like any other document upload," CRR §9) — see Out of scope for the general document capability this piggybacks on.
- Retention-tier classification of the payroll/tax-adjacent fields this module touches (IBAN, tax ID, payslip-request records, wage records) as Tier 3 / 6 years for the Retention module to execute (CRR §25).
- The current-state stub surface actually present in code today: `POST/GET /contracts`, `POST/GET /payroll`, `POST /workers/:worker_id/documents` under `/api/v1/hr`, gated by `hr:read`/`hr:write` permissions (current worktree; see Evidence and Traceability).

**Out of scope:** (owned elsewhere and referenced, never redefined — Constitution §6)

- In-system payroll/salary/hourly-rate calculation, overtime calculation, bonuses & deductions, and payroll exports for finance — confirmed zero payroll math in the system (CRR §23, Explicit Non-Goals).
- Self-retrievable payslips — the worker cannot fetch a payslip themselves; only the request-and-manager-email flow exists (CRR §23).
- QES/e-signature/Skribble integration — contracts are signed by hand; no signature-capture or e-signature provider exists or is planned (CRR §9, §34; PDD §11, Appendix).
- Signature detection, authenticity verification, or any automated check of the handwritten signature — the system trusts the manager's confirmation alone (CRR §9; PDD §7.7 trust boundary).
- The general document-upload/expiry capability beyond the contract scan (other worker document types, non-EU work-permit documents, expiry tracking of those documents) — owned conceptually by the "Documents" capability referenced in CRR §4, §7; this module's upload endpoint is in scope only as the mechanism the contract-scan use case rides on.
- Employee identity, core employment fields, lifecycle status, skill tags, and the profile-and-history view — owned by employee-management (`docs/03-modules/employee-management/MODULE_SPEC.md`, SPEC-EMP-001), which explicitly places "Contract generation, storage, and the manager-confirmed hand-signed contract lifecycle" and "payslip request flow" out of its own scope and into conceptual "Contracts"/"Payslips" modules (SPEC-EMP-001 Out of Scope; CRR §9, §23). This spec is the proposed code-level home for that conceptual surface, **but this proposal directly conflicts with `docs/03-modules/onboarding/MODULE_SPEC.md`'s own first-person ownership claims over the identical CRR §9 behavior — see `OD-HR-01a`, which discloses rather than resolves the conflict.**
- Onboarding orchestration: the Personalfragebogen self-service form, the document-collection chatbot, application → familiarization sequencing, and the pool/claim hire-approval mechanism (Onboarding module; CRR §6, §8, §10).
- Authentication, RBAC/scope framework itself, and the platform account object (Authentication/User Management; CRR §1, §2).
- Immutable audit-log infrastructure and its retention mechanics (shared platform capability; this module only classifies and emits events into it) (CRR §30).
- Automatic tiered retention deletion execution (Retention module; this module only classifies fields into Tier 3) (CRR §25).
- Special-category field policy and payslip-processor visibility governance for Konfession (Compliance/employee-management-adjacent; this module is a consumer of the "payslip-request processor" audience concept, not its definer) (CRR §27; SPEC-EMP-001 `RULE-EMP-09`).

**Non-goals:** (confirmed out of the platform entirely — CRR §23, Explicit Non-Goals; PDD §11, Appendix Non-goals)

- Salary/hourly pay calculation, overtime calculation, bonuses & deductions, and payroll exports for finance.
- Self-retrievable payslips.
- Qualified Electronic Signature (QES) / e-signature integration of any kind (contracts signed by hand).
- Any automated signature detection or document-authenticity verification.

## Evidence and Traceability

| Claim/requirement | Source path, line, revision, or decision | Authority | Status |
|---|---|---|---|
| `REQ-HR-001` System generates a pre-filled contract PDF from Personalfragebogen data and makes it available to download/print | CRR §9 (lines 129-130); PDD §7.7 | Authoritative | Confirmed |
| `REQ-HR-002` Contract signed by hand on paper; no QES/e-signature | CRR §9 (lines 117-119); PDD §11, Appendix | Authoritative | Confirmed |
| `REQ-HR-003` Manager uploads the scanned signed contract and marks "signed contract received & valid"; system trusts this confirmation without verifying the signature | CRR §9 (lines 134-139); PDD §7.7, §8.1 | Authoritative | Confirmed |
| `REQ-HR-004` Account cannot activate until a contract file is uploaded AND marked signed; a visible "contract pending signature" status exists | CRR §9 (lines 137-138); PDD §7.7 | Authoritative | Confirmed |
| `REQ-HR-005` Marking signed starts the 1-year expiry clock and reminder schedule | CRR §9 (line 136); PDD §5.6, §7.7 | Authoritative | Confirmed |
| `REQ-HR-006` Contract lifecycle: 1-year fixed-term with 6-month probation (2-week notice either party during probation) → optional +1-year extension (no new probation) → permanent after 2 years | CRR §9 (lines 141-148) | Authoritative | Confirmed |
| `REQ-HR-007` Manager reminded before 1-year expiry and again before 2-year expiry; no reminders once permanent | CRR §9 (lines 150-153); PDD §5.6 | Authoritative | Confirmed |
| `REQ-HR-008` Signed contracts stored in S3 (EU) | CRR §9 (line 133); PDD §5.7, §7.7 | Authoritative | Confirmed |
| `REQ-HR-009` Worker can request a payslip; manager sends it by email manually; worker cannot self-retrieve | CRR §23 (lines 314-316); PDD §7.7, §4.12 | Authoritative | Confirmed |
| `REQ-HR-010` Zero payroll math performed by the system (no salary/hourly/overtime/bonus/deduction calculation, no finance exports) | CRR §23 (lines 317-321); PDD §4.12 | Authoritative | Confirmed |
| `REQ-HR-011` Contract-scan upload is mechanically treated like any other document upload | CRR §9 (line 132) | Authoritative | Confirmed |
| `REQ-HR-012` Payroll/tax-adjacent fields (IBAN, tax ID, payslip-request records, wage records) retained 6 years, automatic deletion (Tier 3) | CRR §25 (line 335) | Authoritative | Confirmed |
| `REQ-HR-013` Contract storage and payslip-request flow are net-new capabilities (no prior implementation to preserve) | CRR §34 (lines 401, 409) | Authoritative | Confirmed |
| `REQ-HR-014` QES/Skribble integration explicitly removed | CRR §34 (line 410); CRR §9 (line 119) | Authoritative | Confirmed |
| `REQ-HR-015` Application → familiarization (max 2 trial days) → contract sequencing precedes contract conclusion | CRR §9 (lines 121-126); PDD §4.2 | Authoritative | Confirmed |
| Backend code module for this domain is `backend-hr`, currently a stub — every `HrService` method throws `NotImplementedError`; holds no business state | `backend/src/modules/hr/service.ts:5-24`; `.claude/knowledge/DEPENDENCY_GRAPH.yaml:34-36`; `.claude/knowledge/MODULE_REGISTRY.yaml:141-151` | Generated (repo-derived) | Confirmed current-state |
| `NotImplementedError` maps to HTTP 501 | `backend/src/lib/errors.ts:77-82`; `HTTP_STATUS.NOT_IMPLEMENTED` (`backend/src/config/constants.ts:13`) | Generated | Confirmed current-state |
| Routes mounted at `/api/v1/hr`: `GET/POST /contracts` (`hr:read`/`hr:write`), `GET/POST /payroll` (`hr:read`/`hr:write`), `POST /workers/:worker_id/documents` (`hr:write`); all behind `authMiddleware` + `requirePermission` | `backend/src/routes/v1/index.ts:31`; `backend/src/modules/hr/routes.ts:1-30` | Generated (repo-derived) | Confirmed current-state |
| `hr:read`/`hr:write` granted only to `ADMIN` and `MANAGER`; `CHECKER` and `WORKER` hold no `hr:*` permission | `backend/src/config/constants.ts:88-129` (`ROLE_PERMISSIONS`) | Generated (repo-derived) | Confirmed current-state |
| Current-state (pre-`ADR-039`): `CreateContractRequest` fields: `worker_id`, `template_id`, `position`, `salary_amount`, `start_date`, `end_date?`; `CreatePayrollRequest` fields: `worker_id`, `pay_period_start`, `pay_period_end`, `gross_salary`. **Target-state (`ADR-039`, 2026-07-28):** `CreateContractRequest` drops `salary_amount`; `CreatePayrollRequest`/`IF-HR-CreatePayroll` becomes `worker_id`, `period_start`, `period_end`, `status` — no computation fields. | `backend/src/modules/hr/types.ts:1-15` (current-state only; target-state types not yet implemented) | Generated (repo-derived); target-state per `ADR-039` | RESOLVED — target types redesigned (`OD-HR-02`/`ADR-039`); current-state code types remain unimplemented (501) until this redesign lands |
| No `Contract`, `Payroll`, `Payslip`, or HR-specific `Document` Prisma model exists; only User, Session, Hotel, HotelWorker, WorkRequest, WorkApplication, WorkerAssignment, Attendance, QualityVerification, Rating, WorkerOverallRating, Notification, AuditLog | `backend/prisma/schema.prisma` (current worktree, grep-confirmed) | Generated (repo-derived) | Confirmed current-state — this module holds no business state today |
| `mobile-worker` is the only discovered client consumer of `/hr`; no `frontend-web` or `mobile-checker` edge to `backend-hr` exists | `.claude/knowledge/DEPENDENCY_GRAPH.yaml:276` (`edge-mobile-worker-hr`) | Generated (repo-derived) | Confirmed current-state; flagged as a gap — see `OD-HR-05` |
| No frozen module specification currently maps to `backend-hr`; `specification: UNKNOWN` in the registry | `.claude/knowledge/MODULE_REGISTRY.yaml:150` | Generated | Confirmed |
| Conceptual "Contracts" and "Payslips" module boundaries are referenced by SPEC-EMP-001 as out of its scope, but no separate code module or frozen spec exists for either | `docs/03-modules/employee-management/MODULE_SPEC.md` lines 40, 45; `docs/03-modules/contracts/.gitkeep`, `docs/03-modules/payslips/.gitkeep`, `docs/03-modules/documents/.gitkeep` (current worktree) | Generated (repo-derived) | Confirmed — see `OD-HR-01a` |
| `docs/03-modules/onboarding/MODULE_SPEC.md` (an actively-maintained, non-empty specification this spec's v0.1.0 never cited) claims, in first-person ownership language, that **Onboarding** — not `backend-hr` — (a) generates the pre-filled contract PDF, and (b) captures the manager's scanned-upload + "signed & valid" confirmation, for the same CRR §9 behavior this spec assigns to itself in `RULE-HR-01`/`REQ-HR-001` and `RULE-HR-03`/`REQ-HR-003` | `docs/03-modules/onboarding/MODULE_SPEC.md` §5 Responsibilities items 4-5 (lines 87-88); §6.5 "Contract ownership" boundary note (line 187); §7 Onboarding-Lifecycle steps 7-8 (lines 230-231) | Generated (repo-derived); found independently by `FIND-DEP-HR-02` and `FIND-CONS-HR-01` (G4 review) | Confirmed conflict exists — not resolved by this spec; see `OD-HR-01a` |

## Actors and Terminology

| Term/actor | Canonical definition | Source |
|---|---|---|
| Contract | The generated employment-contract PDF plus the stored scanned hand-signed copy and its status/lifecycle metadata | CRR §9; PDD §7.7, §9.3 |
| Contract status | The full confirmed four-state set: `pending` (generated, awaiting upload+manager confirmation) → `signed`/`active` (manager-confirmed) → `extended` (+1 year, no new probation) → `permanent` (open-ended, no further reminders); prior versions of this row cited only the first two states (`FIND-CONS-HR-07`) | CRR §9 |
| Manager confirmation (Option A) | The manager's act of uploading the scan and marking it "signed contract received & valid," trusted without signature verification | CRR §9; PDD §7.7 |
| Familiarization period | Up to 2 trial days before any contract is concluded, to assess mutual fit; precedes contract, not a contract state | CRR §9; PDD §4.2, Appendix |
| Probation period | The 6-month clause inside the initial 1-year fixed-term contract; 2-week notice either party during it; not a separate lifecycle status | CRR §9 |
| Contract extension | The optional +1-year continuation at the 1-year mark; carries no new probation | CRR §9 |
| Permanent contract | The open-ended contract offered after 2 years of successful employment; ends expiry reminders | CRR §9 |
| Payslip request | A worker-initiated request recorded by this module and routed to a manager, who fulfils it by emailing the payslip manually | CRR §23; PDD §7.7 |
| Payslip-request processor | **This row states only the operational identity** — the role/context that handles *fulfilling* payslip requests within this module (Hotel Manager, per current-state RBAC). It does **not** define the Konfession special-category *visibility policy* itself, which remains owned by SPEC-EMP-001/Compliance (`RULE-EMP-09`); this spec is a consumer of that audience concept, not its definer. Per `FIND-CONS-HR-05`, promotion of this term to `TERMINOLOGY.md` must be coordinated with SPEC-EMP-001, not proposed unilaterally by this spec alone — see Proposed Knowledge Deltas. | CRR §27; SPEC-EMP-001 `RULE-EMP-09` |
| Contract-scan document upload | The mechanism by which the scanned signed contract enters the system; mechanically identical to any other document upload | CRR §9 (line 132) |
| Worker (Employee) | The permanent staff role whose contract and payslip requests this module tracks | CRR §1, §4, §9 |
| Hotel Manager | The manager scoped to a single hotel who confirms signed contracts, receives expiry reminders, and fulfils payslip requests for workers at their own hotel; distinct from Regional Manager per SPEC-EMP-001's canonical definitions (split from a merged row per `FIND-CONS-HR-03`) | CRR §1, §9, §23; SPEC-EMP-001 Actors and Terminology |
| Regional Manager | The manager scoped to a Hotel Group/region who confirms signed contracts, receives expiry reminders, and fulfils payslip requests across their group's hotels; distinct from Hotel Manager per SPEC-EMP-001's canonical definitions (split from a merged row per `FIND-CONS-HR-03`) | CRR §1, §9, §23; SPEC-EMP-001 Actors and Terminology |
| Admin | System-wide role; holds `hr:*` permissions alongside Manager in current-state code. **Note (`FIND-CONS-HR-04`, accepted-but-not-fixed):** minor definitional drift of "Admin" against other module specs is a known, tracked item; no edit is made here beyond this note. | `backend/src/config/constants.ts:88-102` |

## Requirements and Acceptance Criteria

| Requirement | Statement | Priority | Acceptance criteria | Rule IDs |
|---|---|---|---|---|
| `REQ-HR-001` | Generate a pre-filled contract PDF from the worker's Personalfragebogen data (name, start date, terms, 1-year fixed term, 6-month probation clause) and make it available to download/print. | MUST | A contract record exists in `pending` status with a generated PDF referencing the confirmed field set; the PDF is downloadable/printable before any signature step. | `RULE-HR-01` |
| `REQ-HR-002` | Never capture, verify, or require an electronic signature; the contract is signed by hand on paper. | MUST | No signature-capture UI, QES/Skribble call, or auto-detection logic exists anywhere in the contract flow. | `RULE-HR-02` |
| `REQ-HR-003` | Record the manager's upload of the scanned signed contract and their explicit "signed contract received & valid" confirmation as the sole trigger that flips contract status to signed/active. | MUST | Status transitions to `signed`/`active` only on a manager-attributed upload + explicit confirmation action; no automated signature check runs; the confirming manager and timestamp are recorded per the concrete audit-event schema in `RULE-HR-15` (`REQ-HR-013`) — not as aspirational prose. | `RULE-HR-02`, `RULE-HR-03`, `RULE-HR-15` |
| `REQ-HR-004` | Block worker-account activation until a contract file is uploaded and marked signed; expose a visible "contract pending signature" status in the interim. | MUST | Attempting activation with no uploaded file, or an uploaded-but-unconfirmed file, fails/blocks; the pending status is queryable by consumers (e.g., Onboarding, employee-management). | `RULE-HR-03`, `RULE-HR-04` |
| `REQ-HR-005` | Start the 1-year expiry clock and reminder schedule at the moment of signed/active confirmation. | MUST | The expiry timestamp is derived from the confirmation event, not from contract generation or upload time. | `RULE-HR-05` |
| `REQ-HR-006` | Own the fixed-term → extension → permanent contract lifecycle exactly as confirmed, with no additional states. | MUST | Only the states `pending → signed/active (1yr) → extended (1yr, no new probation) → permanent` are reachable; no other lifecycle values exist. | `RULE-HR-05`, `RULE-HR-06` |
| `REQ-HR-007` | Remind the responsible manager before the 1-year expiry and again before the 2-year expiry; never after the contract becomes permanent. | MUST | A reminder job fires once before each of the two expiry points for non-permanent contracts; no reminder fires once `permanent` status is reached. | `RULE-HR-07` |
| `REQ-HR-008` | Store the scanned signed contract in S3 (EU) with no data leaving the EU/EEA. | MUST | Stored object resolves to an EU-region S3 bucket/path; no non-EU storage path exists. | `RULE-HR-08` |
| `REQ-HR-009` | Accept a worker's payslip request and route it for manual manager fulfilment by email; never expose a worker-facing payslip download/view. | MUST | A payslip request is recorded with requester and timestamp; no endpoint returns payslip content to the requesting worker; manager-side email-send action is the only fulfilment path. | `RULE-HR-09` |
| `REQ-HR-010` | Perform zero payroll computation (no salary/hourly/overtime/bonus/deduction math, no finance export). | MUST | No calculation logic exists in this module for gross/net pay, overtime, bonuses, or deductions; no export endpoint to finance systems exists. | `RULE-HR-10` |
| `REQ-HR-011` | Treat the contract-scan upload mechanically like any other document upload (same endpoint/mechanism class), without owning general document-expiry tracking. | MUST | The contract-scan upload uses the same `POST /workers/:worker_id/documents`-class mechanism as other document types; no contract-specific expiry-tracking logic for non-contract documents exists here. | `RULE-HR-11` |
| `REQ-HR-012` | Classify IBAN, tax ID, payslip-request records, and wage records as Tier 3 (6-year, automatic deletion) for the Retention module. | MUST | Each named field/record type carries a Tier 3 classification tag consumable by Retention; this module performs no deletion itself. | `RULE-HR-12` |
| `REQ-HR-013` *(added v0.2.0, `FIND-SEC-HR-01`, High)* | Write a concrete, immutable audit event for every manager-confirmation action ("signed contract received & valid"), since this is the sole compensating control for the accepted no-signature-verification trust boundary (CRR §9; PDD §7.7 trust boundary). | MUST | Every confirmation action produces exactly one immutable audit record containing: confirming actor id, timestamp, worker id, contract id, and a reference to the stored evidence file (S3 EU object key). Absence of any one field is a spec violation; the audit record must not be editable or deletable through any application code path. | `RULE-HR-15` |
| `REQ-HR-014` *(added v0.2.0, `FIND-SEC-HR-02`, High)* | Derive the confirming/uploading manager's identity from the authenticated session for both the upload and confirmation actions; never accept it as client-supplied request data. | MUST | `IF-HR-UploadSignedContract` and `IF-HR-ConfirmContractSigned` implementations read the acting manager's identity exclusively from `req.auth` (post-`authMiddleware`); any client-supplied `manager_id`/`confirmed_by`-style field in the request body or path is ignored or rejected, never trusted. | `RULE-HR-14` |
| `REQ-HR-015` *(added v0.2.0, `FIND-SEC-HR-06`, High)* | Validate the scanned/photographed contract file (and, by the same mechanism, any other document upload) for allowed content type and maximum size before storing it, and take an explicit, stated position on malware/content scanning. | MUST | Upload is rejected unless content type is in an allowed image/PDF set and file size is at or under a stated maximum; storage keys are generated server-side and never built from unsanitized client input (e.g., raw `worker_id` path interpolation); the malware/content-scanning position is explicitly stated as deferred (tracked as `OD-HR-14`) rather than left unaddressed. | `RULE-HR-13` |

## Business Rules

| Rule | Preconditions | Outcome/invariant | Exceptions/precedence | Owner/source |
|---|---|---|---|---|
| `RULE-HR-01` | Worker's Personalfragebogen data is available | Contract PDF generated in `pending` status, pre-filled with confirmed fields | **Read-source ambiguity (`OD-HR-02b`):** this row previously stated only "Onboarding" as source, but that is not unambiguous. `IF-HR-GenerateContract`'s own input row hedges "Onboarding/employee-management," and `docs/03-modules/employee-management/MODULE_SPEC.md` (SPEC-EMP-001) states Personalfragebogen-sourced fields are persisted/owned on the employee-management record. The read source is presently ambiguous between Onboarding (transient, during the onboarding flow) and employee-management (persisted record, post-onboarding). Also note: whichever module owns *generation itself* is separately disputed — see `OD-HR-01a`. Neither ambiguity is silently resolved here. | Generation ownership disputed (`OD-HR-01a`); data-source ambiguous (`OD-HR-02b`) (CRR §6, §9) |
| `RULE-HR-02` | Any contract signature event | Signature is always handwritten-on-paper; no e-signature/QES path exists; the system never attempts to verify authenticity | None — explicitly and permanently excluded (CRR §34 line 410) | This module (CRR §9) |
| `RULE-HR-03` | Manager uploads scanned signed contract and confirms "signed & valid" | Contract status flips `pending → signed/active`; confirmation is trusted without verification | No other actor or automated process may flip this status. **Ownership disclosure (`OD-HR-01a`):** `docs/03-modules/onboarding/MODULE_SPEC.md` §5 item 5 claims, in first-person language, that Onboarding — not this module — "accept[s] the scanned/photographed signed paper contract... and record[s] the manager's confirmation." This spec's own claim of ownership over this action is therefore disputed by an actively-maintained sibling spec, not silently resolved here. | This module *claims* (CRR §9; PDD §7.7 trust boundary) — disputed, see `OD-HR-01a` |
| `RULE-HR-04` | Account-activation attempted | Activation permitted only if a contract file exists AND is marked signed; otherwise blocked with a visible "contract pending signature" status | Enforcement point (account activation itself) is owned by Onboarding/employee-management; this module supplies the gating fact | This module supplies fact; Onboarding/employee-management enforce (CRR §9) |
| `RULE-HR-05` | Contract status becomes `signed/active` | 1-year expiry clock starts from that moment; reminder schedule begins | N/A | This module (CRR §9) |
| `RULE-HR-06` | 1-year fixed-term contract reaches expiry and both parties wish to continue | Extended one additional year; no new probation applied | If either party does not wish to continue, contract lapses (offboarding owned elsewhere — see `OD-HR-03`). **RESOLVED (Option (a)) `GD-15`/`ADR-040`, 2026-07-28:** manager-only confirmation — a manager action confirms continuation/permanence at each mark; manager silence past a deadline or an explicit manager "do not continue" action triggers lapse → offboarding. No worker-side approval/veto step. | This module (CRR §9); `ADR-040` |
| `RULE-HR-07` | Contract is non-permanent and approaching its 1-year or 2-year mark | Manager reminded once before each mark | No reminder once contract is `permanent` | This module (CRR §9; PDD §5.6) |
| `RULE-HR-08` | Scanned signed contract file received | Stored in S3 (EU region only) | No non-EU storage path permitted | This module / shared storage infrastructure (CRR §9; PDD §5.7) |
| `RULE-HR-09` | Worker submits a payslip request | Request recorded; routed to responsible manager; manager fulfils by manual email | Worker never receives payslip content through the system itself | This module (CRR §23) |
| `RULE-HR-10` | Any payroll-adjacent computation is contemplated | Rejected — this module performs no salary/hourly/overtime/bonus/deduction calculation and no finance export | None — confirmed non-goal | This module (CRR §23, Explicit Non-Goals) |
| `RULE-HR-11` | Contract-scan file is uploaded | Uses the same upload mechanism class as any other worker document | General document-type expiry tracking (e.g., work permits) is out of scope here | This module uses mechanism; Documents (conceptual) owns general capability (CRR §9 line 132; CRR §7) |
| `RULE-HR-12` | IBAN, tax ID, payslip-request record, or wage record is stored | Classified Tier 3 (6-year retention, automatic deletion) | Deletion execution owned by Retention module | This module classifies; Retention deletes (CRR §25) |
| `RULE-HR-13` *(added v0.2.0, `FIND-SEC-HR-06`/`07`)* | A file is submitted to the contract-scan (or any document-class) upload endpoint | Rejected unless content type is in an allowed image/PDF set and size is at/under a stated maximum; storage key is generated server-side, never from unsanitized client input (e.g., raw `worker_id` path interpolation); **RESOLVED `OD-HR-14`/`ADR-044`, 2026-07-28:** additionally rejected if a synchronous malware scan (run before persistence) detects malicious content. | Malware/content scanning position is **RESOLVED** (`OD-HR-14`/`ADR-044`) — synchronous scan-hook, reject on detection. **Must ship together with, not independently of, the `OD-HR-06` `Buffer.alloc(0)` fix** — both are the same body of upload-handling work. | This module (CRR §9; new — no prior confirmed content-validation requirement, added in response to G4 Security review) |
| `RULE-HR-14` *(added v0.2.0, `FIND-SEC-HR-02`)* | A manager uploads a scanned contract or confirms "signed & valid" | The acting manager's identity is derived exclusively from the authenticated session (`req.auth`) established by `auth-middleware`; never accepted as a client-supplied field | Any client-supplied manager-identity field in the request is ignored/rejected, never trusted | This module (new — no prior confirmed provenance rule, added in response to G4 Security review) |
| `RULE-HR-15` *(added v0.2.0, `FIND-SEC-HR-01`)* | A manager-confirmation ("signed & valid") action occurs | An immutable audit event is written containing: confirming actor id, timestamp, worker id, contract id, and a reference to the stored evidence file (S3 EU object key) | This is the sole compensating control for the accepted no-signature-verification trust boundary (`RULE-HR-02`/`RULE-HR-03`) and must be concrete, not aspirational — "should be logged" is not sufficient | This module (CRR §9, §30; new normative schema, added in response to G4 Security review) |

## Ownership and Boundaries

**Module owner:** `unassigned` — accountable owner assignment is reserved human authority (SYNC-001, blocked: no `CODEOWNERS`, empty `backend/package.json` author). Code-level home is the `backend-hr` module (`backend/src/modules/hr`), currently a stub whose service methods throw `NotImplementedError` (`backend/src/modules/hr/service.ts:5-24`; `.claude/knowledge/DEPENDENCY_GRAPH.yaml:34-36`). This spec is the proposed docs↔code mapping for `backend-hr`; it is not asserted as final — see Proposed Knowledge Deltas and `OD-HR-01a`/`OD-HR-01b`.

**Owned state:**

- The contract record: generated PDF reference, scanned signed-contract file reference (S3 EU), status (`pending` / `signed`-`active` / `extended` / `permanent`), and the manager-confirmation event (who, when).
- Contract lifecycle timers: 1-year expiry, +1-year extension expiry, and the flag that a contract is now permanent (no further reminders).
- Contract expiry-reminder scheduling state (before-1-year, before-2-year, sent/not-sent).
- Payslip requests: requester, timestamp, and fulfilment status (fulfilled-by-manager-email / outstanding).
- Retention-tier classification metadata for the payroll/tax-adjacent fields it touches (IBAN, tax ID, payslip-request records, wage records).

**Consumed state (owned elsewhere, referenced, never redefined):** Personalfragebogen-sourced personal data used to pre-fill the contract PDF (Onboarding); the employee/worker identity and account record that activation ultimately gates (employee-management / Authentication-User Management); general document storage/expiry mechanics for non-contract document types (conceptual Documents capability); the payslip-request-processor and Admin visibility audience for Konfession (Compliance/employee-management-governed policy, this module does not define it); role/scope for `hr:read`/`hr:write` (Authentication/Authorization).

**Permitted writes:** only to this module's owned state above, and only through its own interfaces. This module never writes the employee/employment record, the account/activation flag itself (it exposes the gating *fact*; the actual activation write belongs to Onboarding/employee-management), Personalfragebogen data, or general (non-contract) document records.

**Boundary/non-responsibilities:** this module does **not** perform Onboarding orchestration, chatbot document collection, hire-approval pool/claim, general document-type management or expiry tracking beyond the contract scan, employee lifecycle-status writes, RBAC/scope enforcement itself, audit-log infrastructure, or retention-deletion execution. It holds only the contract-lifecycle and payslip-request state described above and the read/write surface other modules consume. The conceptual "Contracts" and "Payslips" module names used elsewhere in `docs/03-modules/` are **not** separate code modules from `backend-hr` — see `OD-HR-01a`. **This boundary claim is itself disputed** by `docs/03-modules/onboarding/MODULE_SPEC.md`'s first-person ownership language over contract generation and confirmation-capture — see `OD-HR-01a` for the disclosed, unresolved conflict.

## Interfaces and Contracts

For each API, command, query, event, job, or UI contract:

> The authoritative documents describe a modular monolith with synchronous service calls (PDD §5.1-§5.2) but **do not enumerate a formal interface schema** for this module. Current-state code exposes the endpoints below (verified in `backend/src/modules/hr/routes.ts`), which are stubs (`NotImplementedError`, HTTP 501) and whose request/response shapes predate the confirmed contract/payslip model (see `OD-HR-02`). Target-state contracts implied by confirmed behaviour are also listed. Direction is relative to this module.

| Contract ID/version | Direction | Input | Output | Errors | Auth | Compatibility |
|---|---|---|---|---|---|---|
| `IF-HR-GenerateContract / v0` | Inbound (command; target-state, no current route) | Worker id (Personalfragebogen data read from Onboarding **or** employee-management — read-source ambiguity not yet resolved, `OD-HR-02b`) | Contract record in `pending` status + generated PDF reference | Missing Personalfragebogen data `[OPEN]` (`OD-HR-02`, `OD-HR-02b`) | Manager/Admin (candidate — `hr:write`) | New — not yet implemented; **whether this interface belongs to `backend-hr` at all is disputed by `docs/03-modules/onboarding/MODULE_SPEC.md` §5 item 4 — see `OD-HR-01a`** |
| `IF-HR-ListContracts / v0` | Inbound (query) | Query filters (shape unspecified in code). **RESOLVED target scoping `OD-HR-13`/`ADR-043`, 2026-07-28:** available to `MANAGER` (not Admin-only), server-side filtered via `checkWorkerScope()` to the manager's own `hotel_group_id`; `ADMIN` unscoped. | 501 Not Implemented today; target: contract records | None implemented; target errors `[OPEN]` | `hr:read` (ADMIN unscoped; MANAGER scoped via `checkWorkerScope()`, `ADR-043`) | Existing route, stub implementation (`backend/src/modules/hr/routes.ts:10-12`); target scoping now settled |
| `IF-HR-CreateContract / v0` | Inbound (command) | Current-state code shape: `worker_id, template_id, position, salary_amount, start_date, end_date?` (`types.ts:1-8`). **RESOLVED target shape `OD-HR-02`/`ADR-039`, 2026-07-28:** `worker_id, template_id, position, start_date, end_date?` — `salary_amount` removed (contract generation is document production, not compensation-setting). | 501 Not Implemented today | None implemented | `hr:write` (ADMIN, MANAGER only); hotel-scope enforcement required, see `OD-HR-13` | Existing route, stub implementation; target shape now settled (`ADR-039`), not yet implemented |
| `IF-HR-UploadSignedContract / v0` | Inbound (command; target-state) | Worker id, scanned/photographed file (see `RULE-HR-13` for allowed content-type/size/malware-scan position, `OD-HR-14`), manager identity **— MUST be derived from the authenticated session (`req.auth`), never accepted as client-supplied request body/path data (`RULE-HR-14`, `FIND-SEC-HR-02`)** | Contract file stored (S3 EU) under a server-generated storage key (never built from unsanitized client input such as raw `worker_id` path interpolation, `RULE-HR-13`); status still `pending` until confirmed | Missing/unreadable file → manual re-upload (CRR §9); disallowed content type/size → rejected (`RULE-HR-13`) | Manager, Admin; **hotel-scope enforcement (`checkHotelAccess`-equivalent) required before implementation — currently absent, see `OD-HR-13`** | New — current `uploadDocument` stub is the generic mechanism candidate (`backend/src/modules/hr/controller.ts:57-68`); ownership of this interface itself is disputed by `docs/03-modules/onboarding/MODULE_SPEC.md` — see `OD-HR-01a` |
| `IF-HR-ConfirmContractSigned / v0` | Inbound (command; target-state, no current route) | Worker id, confirming manager identity **— MUST be derived from the authenticated session (`req.auth`), never accepted as client-supplied request data (`RULE-HR-14`, `FIND-SEC-HR-02`)** | Contract status → `signed`/`active`; 1-year clock starts; account-activation gating fact flips to satisfied; **a `RULE-HR-15`-compliant audit event is written (confirming actor id, timestamp, worker id, contract id, evidence-file reference) — see `RULE-HR-15` and Observability/audit** | Confirmation without an uploaded file → rejected (CRR §9 safeguard) | Manager, Admin; hotel-scope enforcement required, see `OD-HR-13` | New — not yet implemented; ownership disputed, see `OD-HR-01a` |
| `IF-HR-GetContractStatus / v0` | Inbound (query; target-state, no current route) | Worker id **— for a Worker-role caller this MUST be scoped to the authenticated caller's own identity; never accept `worker_id` as a path/body parameter for Worker-role callers (`OD-HR-10`, `FIND-SEC-HR-03`, IDOR risk)** | Contract status incl. "contract pending signature" visible state, expiry dates | Not found; scope denied | Manager/Regional Manager (their scope), Admin, Onboarding/employee-management (internal read); Worker (self-only, pending `OD-HR-10` resolution — must **not** be satisfied by granting blanket `hr:read`/`hr:write` to `WORKER`); hotel-scope enforcement required for Manager callers, see `OD-HR-13` | New — not yet implemented |
| `IF-HR-UploadDocument / v0 (current-state stub)` | Inbound (command) | `worker_id` (path param); file — current code passes an empty `Buffer.alloc(0)` placeholder, not the actual uploaded file (`controller.ts:59`) | 501 Not Implemented today; target: stored document reference under a server-generated storage key (contract scan is one document type among others per CRR §9's "mechanically treated like any other document upload"); target requires content-type/size validation and a stated malware-scanning position (`RULE-HR-13`, `OD-HR-14`) | None implemented; target errors `[OPEN]` | `hr:write` (ADMIN, MANAGER only); hotel-scope enforcement required, see `OD-HR-13` | Existing route, stub implementation; **current controller does not wire the actual file body** — flagged (`OD-HR-06`). **`OD-HR-06` must not be fixed independently of `RULE-HR-13`'s upload-validation controls (`FIND-SEC-HR-07`) — wiring the real file body and adding content-type/size/storage-key validation ship together, not as separate unrelated tickets.** |
| `IF-HR-RequestPayslip / v0` | Inbound (command; target-state, no current route) | Worker id **— MUST be derived from the authenticated caller's own session identity when the caller is a Worker; never accept `worker_id` as a client-supplied path/body parameter for Worker-role callers (`OD-HR-10`, `FIND-SEC-HR-03`, IDOR risk)** | Payslip-request record (outstanding, routed to manager) | None specified | Worker (self only), and readable by Manager/Admin; hotel-scope enforcement required for Manager-side fulfilment/read, see `OD-HR-13` | New — not yet implemented; must **not** be satisfied by granting blanket `hr:read`/`hr:write` to `WORKER` (`OD-HR-10`) |
| `IF-HR-ListPayroll / v0` | Inbound (query) | **RESOLVED target shape `OD-HR-02`/`ADR-039`, 2026-07-28:** lists payslip-*request* records (`worker_id`, `period_start`, `period_end`, `status`) — not computed payroll data. No "payroll listing" capability exists in CRR/PDD; this interface lists requests only. | 501 Not Implemented today | None implemented | `hr:read` (ADMIN unscoped; MANAGER scoped via `checkWorkerScope()`, **`OD-HR-13`/`ADR-043`, 2026-07-28**) | Existing route; target shape (`ADR-039`) and scoping (`ADR-043`) now settled, not yet implemented |
| `IF-HR-CreatePayroll / v0` | Inbound (command) | Current-state code shape: `worker_id, pay_period_start, pay_period_end, gross_salary` (`types.ts:10-15`) — implied payroll computation, a confirmed non-goal (CRR §23). **RESOLVED target shape `OD-HR-02`/`ADR-039`, 2026-07-28:** `worker_id, period_start, period_end, status` — **no gross-salary or any computation field.** This is a pure payslip-request record; wage calculation happens outside this system. | 501 Not Implemented today | None implemented | `hr:write` (ADMIN, MANAGER only); hotel-scope enforcement required, see `OD-HR-13` | Existing route; target shape now settled (`ADR-039`), removing the zero-payroll-math conflict |
| `IF-HR-FulfilPayslipRequest / v0` | Inbound (command; target-state, no current route) | Payslip-request id, manager action (marks emailed) | Request status → fulfilled | Request not found | Manager, Admin | New — not yet implemented |
| `IF-HR-ContractExpiryReminder / v0` | Outbound (scheduled job → Notifications) | Contract nearing 1-year or 2-year mark | Reminder notification to responsible manager | Contract already permanent → no reminder sent | Internal (scheduled job) | New — target-state (PDD §5.6) |

## Events

> No event bus, message broker, or publish/subscribe mechanism exists in the repository (`.claude/knowledge/DEPENDENCY_GRAPH.yaml:409-416`; `MODULE_REGISTRY.yaml` records `published_events: none-observed` / `consumed_events: none-observed` for `backend-hr`). The following are business-level domain events implied by owned state changes; concrete event contract/transport is an open decision (`OD-HR-04`).

| Event ID/version | Publisher | Trigger | Payload source | Consumers | Delivery/idempotency |
|---|---|---|---|---|---|
| `EVT-HR-ContractGenerated / v0` | backend-hr | Contract PDF generated (`pending`) | Worker id, contract id | Onboarding, Audit | `[OPEN]` (`OD-HR-04`) |
| `EVT-HR-ContractSigned / v0` | backend-hr | Manager confirms "signed & valid" | Worker id, contract id, confirming manager | Onboarding, employee-management (account activation gate), Notifications, Audit | `[OPEN]` |
| `EVT-HR-ContractExtended / v0` | backend-hr | Both parties continue at 1-year mark | Worker id, contract id, new expiry | Notifications, Audit | `[OPEN]` |
| `EVT-HR-ContractMadePermanent / v0` | backend-hr | 2 years reached, offer accepted | Worker id, contract id | Notifications, Audit | `[OPEN]` |
| `EVT-HR-ContractExpiryReminderDue / v0` | backend-hr | Scheduled job hits 1-year or 2-year pre-mark | Worker id, manager id, contract id | Notifications, Audit | `[OPEN]` |
| `EVT-HR-PayslipRequested / v0` | backend-hr | Worker requests a payslip | Worker id, request id | Notifications (manager), Audit | `[OPEN]` |
| `EVT-HR-PayslipFulfilled / v0` | backend-hr | Manager marks request emailed | Request id, manager id | Audit | `[OPEN]` |

**Consumed events** (contract `[OPEN]`, `OD-HR-04`): Onboarding — Personalfragebogen completion signal to trigger contract generation (CRR §6, §9); employee-management/Onboarding — worker identity/account context needed to gate activation (CRR §9). No events are currently consumed in code (`published_events: none-observed`, `consumed_events: none-observed` for `backend-hr`, `.claude/knowledge/MODULE_REGISTRY.yaml:148-149`).

## Dependencies

| Dependency/edge | Reason | Contract | Compatibility | Failure behavior |
|---|---|---|---|---|
| Onboarding | Supplies Personalfragebogen data for PDF pre-fill; signals application → familiarization → contract sequencing. **Ownership dispute:** `docs/03-modules/onboarding/MODULE_SPEC.md` claims to perform contract generation and confirmation-capture itself — see `OD-HR-01a` | `IF-HR-GenerateContract` (candidate) | `UNKNOWN` (reclassified from `conditional` per `FIND-DEP-HR-01`: "conditional" implies a real counterpart contract exists; `Onboarding` is not a registered module id in `MODULE_REGISTRY.yaml`, so no counterpart contract can be compared against; schema also `[OPEN]`, `OD-HR-04`) | No source data → contract cannot be generated |
| Employee-management (`docs/03-modules/employee-management/MODULE_SPEC.md`, `backend-hr` docs↔code overlap) | Consumes this module's contract-signed gating fact to permit account activation; SPEC-EMP-001 lists "Contract, storage... lifecycle" as consumed/referenced state it does not own | `IF-HR-GetContractStatus` (candidate) | `UNKNOWN` (reclassified from `conditional` per `FIND-DEP-HR-01`: `employee-management` is not a registered module id — see `MODULE_REGISTRY.yaml`; no counterpart contract exists to compare against) | No signed contract → employee cannot activate (CRR §9) |
| Documents — **reworded per `FIND-DEP-HR-01`: this is not a cross-module dependency.** The row previously mislabeled this as an external edge; it in fact describes **this module's own upload endpoint** (`POST /workers/:worker_id/documents`, `backend/src/modules/hr/routes.ts:26-28`), which the contract-scan flow reuses as an intra-module mechanism. "Documents" as a conceptual capability (CRR §4/§7) has no code module or frozen spec and is referenced only for the general-document-type scope this module does *not* implement (see Out of Scope) | Contract-scan upload rides the same intra-module upload mechanism (`IF-HR-UploadDocument`); not an external dependency | not applicable — intra-module mechanism, not a cross-module edge | Upload mechanism unavailable → scan cannot be attached (self-contained failure, not a cross-module one) |
| Authentication / Authorization (`auth-middleware`, `permissions-middleware`) | `authMiddleware` + `requirePermission('hr:read'/'hr:write')` gate every route. **Correction (ADR-030 PR-1, `OD-HR-13` partially resolved):** the three worker_id-carrying write routes (`POST /contracts`, `POST /payroll`, `POST /workers/:worker_id/documents`) now also gate on `requireRole(['admin','manager'])` and a new `checkWorkerScope()` middleware (`backend/src/middleware/permissions.ts`), which denies a manager whose scope does not cover the target worker's `EmploymentRecord.hotel_group_id`. The two list routes (`GET /contracts`, `GET /payroll`) carry no `worker_id`/`hotel_id` and no query-filter schema exists to scope-filter against, so they were narrowed to `requireRole('admin')` rather than exposed to `MANAGER` unscoped — this is a deliberate product decision, not a completion of `OD-HR-13` for the read side. **Sequencing risk (`FIND-SEC-HR-08`):** `docs/03-modules/auth/MODULE_SPEC.md`'s own security review returned FAIL (1 Critical account-takeover, 4 High) — see `OD-HR-15` | `auth-middleware`, `permissions-middleware` (reused) | compatible (reuse) | No auth/permission → all `/hr` routes return 401/403 |
| `base-service` (Prisma access + audit logging) | `HrService extends BaseService` for shared Prisma client and audit hooks | `base-service` (reused) | compatible (reuse) | Standard platform failure modes |
| Notifications | Delivery of expiry-reminder and payslip-request notifications to managers | `notification-service` (candidate; not yet wired — `backend-hr` has zero outbound edges today) | conditional (`notification-service` IS a registered contract id, owned by `backend-notifications` — a real counterpart exists, so `conditional` remains appropriate here, unlike the unregistered targets above) | Reminder/notification undelivered; contract/payslip state unaffected |
| Retention | Executes automatic Tier 3 (6-year) deletion of classified payroll/tax-adjacent fields | classification metadata (this module) | `UNKNOWN` (reclassified from `conditional` per `FIND-DEP-HR-01`: `Retention` is not a registered module id in `MODULE_REGISTRY.yaml`) | Fields not deleted on schedule (compliance risk) |
| Compliance / employee-management (Konfession policy) | Defines the "payslip-request processor" audience this module's fulfilment role participates in | referenced (policy owned elsewhere) | `UNKNOWN` (reclassified from `conditional` per `FIND-DEP-HR-01`: `Compliance` is not a registered module id in `MODULE_REGISTRY.yaml`) | Special-category visibility audience incomplete without that definition |
| `mobile-worker` (Employee App) | Only discovered client consumer of `/hr` (`edge-mobile-worker-hr`) | consumes-api (observed) | unknown (`.claude/knowledge/DEPENDENCY_GRAPH.yaml:276`) | No manager-facing (`frontend-web`) consumer edge is recorded even though CRR §9/PDD §7.7/§8.1 describe manager actions (uploading scan, marking signed, emailing payslips) — see `OD-HR-05` |
| Platform/infrastructure | PostgreSQL system of record, Express/TS monolith, immutable audit log, Winston, AWS, S3 (EU) object storage | shared infrastructure (`base-service`, `prisma-schema`) | compatible (reuse) | Standard platform failure modes |

**Conceptual dependency-cycle note (`FIND-DEP-HR-04`, disclosed not resolved):** if the edges each of the three sibling specs currently claims were all added to `DEPENDENCY_GRAPH.yaml` as proposed — `backend-hr` → `employee-management` (this spec, contract-signed gating fact), `employee-management` → `Onboarding` (SPEC-EMP-001, activation-decision relay), and `Onboarding` → `backend-hr` (`docs/03-modules/onboarding/MODULE_SPEC.md` §6.5, ongoing-lifecycle reference back to "the Contracts/HR module") — the result is a latent 3-node conceptual cycle (`backend-hr → employee-management → Onboarding → backend-hr`). This is flagged for architecture attention at the next dependency-synchronization pass; it is not resolved by this spec.

## State and Lifecycle

**States (contract-level, owned here):**

- **(none) → Pending** — contract PDF generated from Personalfragebogen data; awaiting print, hand-signature, scan upload, and manager confirmation (CRR §9).
- **Pending (visible "contract pending signature")** — file may or may not yet be uploaded; account activation blocked throughout (CRR §9 safeguard).
- **Signed / Active** — manager has uploaded the scan and confirmed "signed contract received & valid"; 1-year expiry clock and reminder schedule start; this is what unblocks account activation (CRR §9).
- **Extended** — at the 1-year mark, both parties continue; +1 year added; no new probation (CRR §9).
- **Permanent (open-ended)** — after 2 years of successful employment; no further expiry reminders (CRR §9).

**States (payslip-request-level, owned here):**

- **Requested** — worker submits a request.
- **Fulfilled** — manager marks the request as emailed to the worker.

**Transitions (each arises from a confirmed event; audit-logging expectation per Constitution §11, mirrored from `RULE-EMP-11`-style practice — concrete implementation `[OPEN]`, `OD-HR-04`):**

- `(none) → Pending` — contract PDF generated (triggered by Onboarding completion signal, `[OPEN]` transport, `OD-HR-04`).
- `Pending → Signed/Active` — manager uploads scan + confirms "signed & valid"; system does not verify the signature itself (CRR §9).
- `Signed/Active → Extended` — both parties agree to continue at the 1-year mark; **the mechanism for capturing "both parties agree" is unspecified (`OD-HR-07`)**.
- `Extended → Permanent` — after 2 years of successful employment, permanent contract offered; **the trigger/decision mechanism is unspecified (`OD-HR-07`)**.
- `Requested → Fulfilled` (payslip) — manager marks the request emailed.

**Invariants:** no contract may reach `Signed/Active` without both an uploaded file and an explicit manager confirmation action (CRR §9 safeguard); no reminder is sent once a contract is `Permanent`; the system never writes a payroll computation result anywhere in this state (CRR §23); a worker-facing payslip-content read path must never exist (CRR §23).

**Concurrency:** not addressed in the authoritative documents for this module specifically; PDD §7.2 describes an atomicity pattern (single transaction) for a different module (Calendar/sick-vacation) that may be an analogous concern here but is not confirmed as applying to contract confirmation — labelled unknown (Constitution §6, `OD-HR-08`).

**Retention/migration:** payroll/tax-adjacent fields (IBAN, tax ID, payslip-request records, wage records) are Tier 3, retained 6 years then automatically deleted by Retention (CRR §25). No prior `Contract`/`Payroll`/`Payslip` data exists in the current schema to migrate — this is entirely net-new capability, built pre-launch with no production data (CRR §34; PDD §10).

## Failure, Security, Privacy, and Performance

**Failure modes/recovery:**

- Contract confirmation attempted without an uploaded file → rejected; account stays un-activatable (CRR §9 safeguard).
- Uploaded scan is bad/blurry/unreadable → manual re-upload required; no automated quality check exists (PDD §7.7 failure modes).
- Manager attempts to mark signed without having actually reviewed the paper contract → the system cannot detect this; it is an accepted trust boundary, not a failure mode the system guards against (CRR §9; PDD §7.7 trust boundary).
- Payslip request submitted but manager never fulfils it → no confirmed escalation/reminder behaviour exists for this case — labelled unknown (`OD-HR-09`).
- Current-state: every route returns HTTP 501 (`NotImplementedError`) because no business logic exists yet (`backend/src/modules/hr/service.ts:5-24`).

**Trust boundaries/authorization:** the system does **not** capture, verify, or auto-detect the handwritten signature — it records only the manager's confirmation and the stored evidence file (CRR §9; PDD §7.7 trust boundary, Constitution §11 applies: this is the single most important trust-boundary statement for this module and must not be silently strengthened or weakened by implementation). The sole compensating control for this trust boundary is the concrete audit-event schema in `RULE-HR-15`/`REQ-HR-013` (`FIND-SEC-HR-01`) — the manager's confirmation must be attributable and immutable precisely because the system cannot independently verify what is being confirmed. Deny-by-default authorization on role: current code grants `hr:read`/`hr:write` only to `ADMIN` and `MANAGER` roles; `CHECKER` and `WORKER` hold no `hr:*` permission at all (`backend/src/config/constants.ts:88-129`). This means a worker cannot, under current-state RBAC, even read their own contract status or submit a payslip request through the `hr:*`-gated routes — **this is a gap against confirmed behaviour** (CRR §9's implicit need for worker-side contract visibility during signing, and CRR §23's explicit "worker can REQUEST a payslip") and is recorded as `OD-HR-10`, not silently resolved. **Whoever resolves `OD-HR-10` must scope any worker-facing `worker_id` parameter to the authenticated caller's own identity — never accept it as a path/body parameter for Worker-role callers — for both `IF-HR-RequestPayslip` and `IF-HR-GetContractStatus`, and must NOT resolve the gap by extending blanket `hr:read`/`hr:write` to `WORKER` (`FIND-SEC-HR-03`, IDOR risk).**

**Hotel-scoping gap (`FIND-SEC-HR-04`, High — new finding, not previously identified in v0.1.0).** Confirmed at `backend/src/modules/hr/routes.ts` (all `/hr` routes) and `backend/src/middleware/permissions.ts`. **Correction (ADR-030 PR-1, `OD-HR-13` partially resolved; extended PR #338 — see 2026-08-05 byproduct correction below):** all `hr:*` admin/manager-class routes gate on `requireRole([...,'regional_manager'])` and `checkWorkerScope()`, denying a manager or regional_manager whose scope does not cover the target worker's employment-record group. Both list routes (`GET /contracts`, `GET /payroll`) admit `regional_manager` group-scoped through the same seam — list-route *scope-filtering* (as opposed to role admission) remains open per `OD-HR-13`, but role admission itself is built for every row with a current route. `OD-HR-13` remains open for: (a) list-route scope-filtering refinement, and (b) the target-state interfaces below that have no current route at all.

Permission matrix (against the reused five-role RBAC model; deny-by-default). **"Their hotel's workers" / "their group" in the Manager/Regional Manager columns below is the *confirmed intent*.** For every current-state route (write and list) it is now also the enforced fact (`ADR-030` PR-1, extended PR #338 — see 2026-08-05 byproduct correction); for the target-state rows below that have no current route at all, it remains intent only, unenforced because unbuilt.

| Capability | Staff (Worker) | Checker | Hotel Manager | Regional Manager | Admin |
|---|---|---|---|---|---|
| View own contract status ("pending signature" / signed / expiry) | `[OPEN]` — no `hr:*` permission granted today (`OD-HR-10`); if resolved, MUST be self-scoped only (never a client-supplied `worker_id`, `FIND-SEC-HR-03`) | — | ✅ (their hotel's workers — intent only, no current route, `OD-HR-13`) | ✅ (their group — intent only, no current route, `OD-HR-13`) | ✅ |
| Upload scanned signed contract | — (CRR §9: manager uploads) | — | ✅ (their hotel's workers — scope enforced via `checkWorkerScope()`, ADR-030 PR-1) | ✅ (their group — same enforcement) | ✅ |
| Confirm "signed contract received & valid" | — | — | ✅ (their hotel's workers — intent only, no current route, `OD-HR-13`) | ✅ (their group — intent only, no current route, `OD-HR-13`) | ✅ |
| Receive expiry reminders (1yr / 2yr) | — | — | ✅ (responsible manager) | ✅ | ✅ |
| Request a payslip | `[OPEN]` — no `hr:*` permission granted today (`OD-HR-10`); CRR §23 confirms worker CAN request; if resolved, MUST be self-scoped only (`FIND-SEC-HR-03`) | — | — | — | — |
| Fulfil (email) a payslip request | — | — | ✅ (their hotel's workers — intent only, no current route, `OD-HR-13`) | ✅ (their group — intent only, no current route, `OD-HR-13`) | ✅ |
| Create a contract / payroll record (write, current-state routes) | — | — | ✅ (their hotel's workers — scope enforced via `checkWorkerScope()`, ADR-030 PR-1) | ✅ (their group — same enforcement) | ✅ |
| View/list contracts, payroll records (read, current-state routes) | — | — | ✗ (narrowed to Admin-only, ADR-030 PR-1 — no query-filter schema exists yet to scope-filter a manager's view) | ✗ (same) | ✅ |

**Data classification/retention:** Germany-only; scanned contracts and any photo evidence stored in EU/EEA S3 — no data leaves the EU/EEA (CRR §9; PDD §5.7). Retention tiers touched by this module: Tier 3 — payroll/tax-adjacent fields (IBAN, tax ID, payslip-request records, wage records) — 6 years, automatic deletion (CRR §25). Contract documents themselves are not explicitly assigned a retention tier in CRR/PDD; treating them as Tier 2 (general personal/profile data, 5 years) or Tier 3 is unresolved — see `OD-HR-11`. Special-category adjacency: Konfession visibility for the "payslip-request processor" role is governed by employee-management/Compliance policy, not defined here (CRR §27; SPEC-EMP-001 `RULE-EMP-09`) — this module must not independently redefine that audience.

**Performance budgets/workload:** no explicit SLO is defined in the authoritative documents for contract generation, upload, or payslip-request handling. Design intent: contract-expiry reminder and payslip-request volume scale with employee headcount, not with request rate (low-frequency, per-employee-per-year events). Concrete budgets are deferred to implementation and are not asserted here (labelled unknown, Constitution §6).

**Observability/audit:** every contract-status transition, manager confirmation, and payslip-request/fulfilment action should be logged immutably via the existing audit framework, consistent with the platform-wide "every important action is logged" rule (CRR §30) — no HR-specific audit behaviour beyond that is confirmed, and current-state code has no audit calls implemented (`backend/src/modules/hr/service.ts` throws before any logic executes). No admin-facing log-viewer screen exists platform-wide (CRR §30). **Strengthened per `FIND-SEC-HR-01` (High):** for the manager-confirmation action specifically, "should be logged" is insufficient on its own — `RULE-HR-15`/`REQ-HR-013` mandate a concrete, normative audit-event schema (confirming actor id, timestamp, worker id, contract id, evidence-file reference), written immutably, as the sole compensating control for the accepted no-signature-verification trust boundary. This schema requirement is not aspirational prose; it is a MUST-level acceptance criterion.

## Rollout and Compatibility

This module is entirely net-new capability within the marketplace → Workforce Operations Platform forward refactor (CRR §34 lines 401, 409; PDD Phase 3, Milestone M4). It is pre-launch with no production employee, contract, or payslip data, so no dual-run migration is needed (PDD §10). Sequence: this module's capabilities are built in PDD Phase 3 ("Compliance & onboarding"), depending on Phase 1 foundation realignment (Regional Manager role/scope, response-envelope centralization) already assumed complete for RBAC checks to function (PDD §10 Phase 1, Phase 3; roadmap M1, M4).

**Feature flags:** each new capability (contract generation, manager-confirmation flow, expiry reminders, payslip-request flow) sits behind the existing `FEATURE_*` env-flag convention so partial deploys are safe (PDD §10).

**Backward compatibility:** the current-state `CreateContractRequest`/`CreatePayrollRequest` types and the `/contracts`, `/payroll` routes do not match the confirmed target model (see `OD-HR-02`) and carry no production data or external contract to preserve — replacing them is not a breaking change against any live consumer (`mobile-worker` currently receives only 501 responses from these routes).

**Rollback:** because this is additive, pre-launch work, rollback is disabling the relevant feature flag and redeploying the prior (stub) build (PDD §10).

**Removal criteria:** not applicable — contract lifecycle and payslip-request handling are confirmed, permanently-owned legal/compliance capabilities (CRR §9, §23).

## Validation Plan

| Criterion | Test level/check | Environment/data | Evidence required |
|---|---|---|---|
| Contract PDF generated in `pending` status from confirmed field set (`REQ-HR-001`) | Unit + fixture | Seed Personalfragebogen data | Generated PDF reference + field-presence check |
| No e-signature/QES code path exists anywhere in the contract flow (`REQ-HR-002`) | Static/code-presence check | Codebase scan | Absence of QES/Skribble/signature-capture calls |
| Status flips to signed/active only via manager upload + explicit confirmation; no auto-verification (`REQ-HR-003`) | Unit + integration | Manager-role fixture | Transition trace showing manager attribution; absence of signature-detection call |
| Activation blocked without uploaded+confirmed contract; "pending signature" status visible (`REQ-HR-004`) | Integration | Unsigned / signed contract fixtures | Blocked-activation assertion; visible status field in query response |
| 1-year clock starts exactly at confirmation event (`REQ-HR-005`) | Unit | Confirmation timestamp fixture | Expiry date derivation check |
| Only confirmed lifecycle states reachable; no additional states (`REQ-HR-006`) | Unit (state machine) | Full lifecycle fixture | Transition-table coverage incl. negative cases |
| Reminder fires once before 1yr and once before 2yr; none once permanent (`REQ-HR-007`) | Unit (scheduled job) | Contract-age fixtures at each boundary | Reminder-fired/not-fired assertions per state |
| Scanned contract stored only in EU S3 region (`REQ-HR-008`) | Integration/config check | Storage config | Bucket-region assertion |
| Payslip request recorded; no worker-facing payslip-content read path exists (`REQ-HR-009`) | Integration + route-absence check | Payslip-request fixture | Request record; absence of worker-facing content-return route |
| No payroll computation logic exists anywhere in the module (`REQ-HR-010`) | Static/code-presence check | Codebase scan | Absence of salary/overtime/bonus/deduction calculation code and finance-export route |
| Contract-scan upload uses the same mechanism class as other document uploads (`REQ-HR-011`) | Integration | Multi-document-type fixture | Shared-mechanism evidence |
| IBAN/tax ID/payslip-request/wage-record fields carry Tier 3 classification (`REQ-HR-012`) | Unit (classification) | Field catalog | Tier-mapping table verified |
| Every manager-confirmation action writes exactly one immutable audit record with all five mandated fields (confirming actor id, timestamp, worker id, contract id, evidence-file reference); record is never editable/deletable (`REQ-HR-013`) | Unit + integration | Confirmation-action fixture | Audit-record field-presence assertion; edit/delete-attempt rejection evidence |
| Manager identity for upload/confirm actions is always read from `req.auth`; a client-supplied `manager_id`/`confirmed_by`-style field is ignored or rejected, never trusted (`REQ-HR-014`) | Unit + integration (authorization) | Authenticated-manager fixture with a forged body/path identity field | Assertion that the persisted/audited actor id matches the session, not the forged field |
| Upload is rejected outside the allowed content-type/size bounds; storage key is server-generated, never built from unsanitized client input; malware-scan position is explicitly the deferred `OD-HR-14` stance, not silent (`REQ-HR-015`) | Integration + static/config check | Valid/invalid file-type and oversize fixtures; a `worker_id` crafted for path-injection | Accept/reject assertions per content-type/size boundary; storage-key construction inspection showing no raw client-input interpolation |

## Risks, Assumptions, and Open Decisions

| ID | Type | Description | Evidence/impact | Owner | Resolution/status |
|---|---|---|---|---|---|
| `OD-HR-01a` *(v0.2.0, expanded from `OD-HR-01`; `FIND-CONS-HR-01` High, `FIND-DEP-HR-02` High, `FIND-ARCH-HR-01` Medium — three independent reviewers converge on this)* | Open decision — **RESOLVED** | **Two active specifications currently assign the identical CRR §9 behavior (contract generation; manager-confirmation capture) to two different modules.** Side-by-side quotes: **This spec** — `RULE-HR-01`/`REQ-HR-001`: "This module generates [the contract PDF]..."; `RULE-HR-03`/`REQ-HR-003`: "This module... Record the manager's upload..." (§ Business Rules, this document). **`docs/03-modules/onboarding/MODULE_SPEC.md`** — §5 Responsibilities item 4: "Contract generation & presentation..." (line 87); item 5: "Hand-signed contract capture..." (line 88); §6.5 boundary note (line 187); §7 Onboarding-Lifecycle steps 7-8 (lines 230-231). **RESOLVED `ADR-012` (Accepted, 2026-07-12), documentation-synchronization correction applied 2026-07-28:** `backend-hr`/`SPEC-HR-001` is the ratified canonical owner of contract generation, contract templates, contract metadata, versions, amendments, signatures, and manager confirmation. Onboarding's §5 items 4-5 and §7 steps 7-8 first-person ownership language is **superseded** — Onboarding coordinates/triggers these steps as a consumer of `backend-hr`'s interfaces, it does not own them (`ADR-012` Decision point 3). This document's own text was never updated to cite `ADR-012` until this correction; the conflict was never live after 2026-07-12, only undocumented as resolved. | This is not a documentation nit — it was a live, unresolved ownership conflict over a legally load-bearing action (account activation gate, CRR §8/§9), now resolved. | Human/Architecture (`ADR-012`) | **RESOLVED — `backend-hr` is the ratified owner; Onboarding's overlapping claim is superseded, not authoritative.** |
| `OD-HR-01b` *(v0.2.0, new; `FIND-ARCH-HR-01` Medium)* | Open decision | Resolving `OD-HR-01a` (the module-boundary question: this spec vs. Onboarding vs. the conceptual "Contracts"/"Payslips" directories) requires a formal Architecture Decision Record or a ratified `DECISION_INDEX.md` entry — it is **not** resolvable inside either module specification document. Neither this spec's author nor the onboarding spec's author has the authority to unilaterally settle it by editing their own document. **RESOLVED:** `ADR-012` is exactly that formal Decision Record — ratified 2026-07-12. | Blocks G2 freeze for this spec (and, by the same logic, for the onboarding spec's overlapping sections) until the ADR/decision record exists and is ratified. **No longer blocking** — the ADR exists and is ratified; G2 freeze itself remains a separate, still-outstanding step pending this document's other open items. | Human/Architecture | **RESOLVED — `ADR-012` is the required Decision Record, ratified 2026-07-12.** |
| `OD-HR-02` | Open decision | Current-state code types/routes (`CreateContractRequest` implies template/salary fields; `CreatePayrollRequest` implies gross-salary/pay-period computation) do not match the confirmed target model (generate→scan-upload→manager-confirm contract flow; payslip *request*-only, zero payroll math). **RESOLVED (Option (a)) `GD-15`/`ADR-039`, 2026-07-28:** target-state types redesigned. `CreateContractRequest` retains only fields needed for PDF generation from Personalfragebogen data (`worker_id`, `template_id`, `position`, `start_date`, `end_date?`) — `salary_amount` removed, since contract generation is document production, not compensation-setting. `CreatePayrollRequest`/`IF-HR-CreatePayroll` becomes a pure payslip-request record (`worker_id`, `period_start`, `period_end`, `status`) — **no gross-salary/pay-period computation field of any kind.** Actual wage calculation happens outside this system (external payroll provider or manual process); HR tracks only that a payslip was requested and its fulfillment status. `IF-HR-ListPayroll` lists these request records, not computed payroll data. | High impact: `IF-HR-CreatePayroll`/`IF-HR-ListPayroll` as currently typed directly conflict with the confirmed zero-payroll-math non-goal (CRR §23) if implemented as-is. | Architecture/Human | **RESOLVED — target types redesigned to remove all payroll-computation fields, `ADR-039`; no production data or external contract affected (current routes return 501 only)** |
| `OD-HR-02b` *(v0.2.0, new; `FIND-ARCH-HR-02` Medium)* | Open decision | Personalfragebogen data-source ambiguity for contract pre-fill: `RULE-HR-01` and the Dependencies table state only "Onboarding" as the supplier, but `IF-HR-GenerateContract`'s own input row hedges "Onboarding/employee-management," and `docs/03-modules/employee-management/MODULE_SPEC.md` (SPEC-EMP-001) states this data is persisted/owned on the employee-management record post-onboarding. | The read source is genuinely ambiguous between Onboarding (transient, during onboarding) and employee-management (persisted record). This spec does not silently pick one — see `RULE-HR-01` and `IF-HR-GenerateContract`. | Architecture/Human | Open — flagged, not resolved; likely resolves alongside `OD-HR-01a` since both concern the same generation step |
| `OD-HR-03` | Open decision | Contract lapse / non-renewal / offboarding workflow when a party does not wish to continue at the 1-year or 2-year mark is undefined in CRR/PDD. **RESOLVED (Option (a)) `GD-15`/`ADR-040`, 2026-07-28:** manager-only confirmation. Manager declines or fails to confirm continuation by a deadline → contract lapses → triggers offboarding. **Downstream mechanism now also resolved:** `SPEC-EMP-001`'s `OD-EMP-04`/`ADR-045` (2026-07-28) makes this lapse trigger `employee-management`'s `Deactivated` transition automatically, via a direct in-process call (`ADR-032`). No worker-side approval/veto step. | Affects `RULE-HR-06` exception handling and any downstream employee-lifecycle deactivation trigger — now resolved end-to-end via `SPEC-EMP-001`'s `OD-EMP-04`/`ADR-045`. | Product/Human | **RESOLVED — manager-only confirmation/lapse (`ADR-040`), automatically triggering `Deactivated` via `ADR-045`; end-to-end mechanism now fully resolved** |
| `OD-HR-04` | **RESOLVED (`GD-12`/`ADR-032`, 2026-07-28)** | Formal domain-event/interface transport: direct in-process call for synchronous effects, the existing Outbox for asynchronous/durable effects, no generic event bus (matches `SPEC-EMP-001`'s identical `OD-EMP-09` resolution). Per-event mechanism assignment for `EVT-HR-*` remains deferred until each event's producer is actually built — this module remains a stub regardless (`OD-HR-01a`/`GD-15`). | Affects Interfaces and Contracts, Events sections; mirrors SPEC-EMP-001 `OD-EMP-09`. | Architecture/Human | **RESOLVED — transport convention set (`ADR-032`); does not unblock this module's own build, still gated on `OD-HR-01a`/`GD-15`** |
| `OD-HR-05` | Open decision | No `frontend-web` (manager-facing web app) or `mobile-checker` consumer edge to `backend-hr` is recorded in `DEPENDENCY_GRAPH.yaml`, even though CRR §9/PDD §7.7/§8.1 describe manager actions (uploading scans, confirming signatures, sending payslip emails) that a manager-facing client would need to perform. Only `mobile-worker` consumes `/hr` today. | Gap between confirmed manager-driven behaviour and discovered client integration; manager-side flows may not yet exist in any client codebase. | Architecture/Human | Open — flagged as a client-integration gap, not resolved by this spec |
| `OD-HR-06` | Open decision | The current `uploadDocument` controller method hardcodes `Buffer.alloc(0)` instead of the actual request body/file (`backend/src/modules/hr/controller.ts:59`), meaning even if the service were implemented, no real file would ever be persisted. | Current-state code defect noted for downstream implementation/architecture review; not a requirements gap. **Linked per `FIND-SEC-HR-07`: this must not be fixed independently of `RULE-HR-13`'s upload-validation controls (content-type/size/malware-scan-position/storage-key generation) — they ship together, not as separate unrelated tickets.** `RULE-HR-13`'s malware-scan position is now RESOLVED (`OD-HR-14`/`ADR-044`) — the scan-hook must be wired in the same change that fixes this defect. | Architecture/Human | Open (code defect, not a decision) — remediated at implementation time, bundled with `RULE-HR-13`'s now-resolved validation controls per `ADR-044` |
| `OD-HR-07` | Open decision | The mechanism for capturing "both parties wish to continue" (at the 1-year mark) and "employee offered permanent contract" (at 2 years) — e.g., a manager action, a worker confirmation, or both — is not specified in CRR/PDD. **RESOLVED (Option (a)) `GD-15`/`ADR-040`, 2026-07-28:** manager-only confirmation — a single manager action confirms continuation/permanence; no worker-side confirmation step. Mirrors the platform's existing manager-write-authority pattern (`ADR-030`) for employment-relationship decisions. | Affects `RULE-HR-06` and the `Signed/Active → Extended → Permanent` transitions in State and Lifecycle. | Product/Human | **RESOLVED — manager-only confirmation, `ADR-040`** |
| `OD-HR-08` | Open decision | Concurrency behavior for contract confirmation (e.g., two managers acting on the same worker's contract simultaneously) is not addressed in CRR/PDD for this module specifically. | Low-probability but unaddressed race condition; no confirmed mitigation exists to cite (unlike the broadcast-slot-lock pattern documented for Job Dispatch). | Architecture/Human | Open — labelled unknown per Constitution §6, not assumed |
| `OD-HR-09` | Open decision | No escalation/reminder behaviour is confirmed for a payslip request that a manager never fulfils. **RESOLVED (Option (a)) `GD-15`/`ADR-041`, 2026-07-28:** time-based auto-escalation — if unfulfilled after **3 business days** (configurable, not hard-coded), the request escalates to a notification to the manager's own manager, or Admin if no reporting-structure manager exists, via the existing Outbox (`ADR-029`). The "manager's own manager" lookup depends on `GD-03`'s still-open org-chart data model (`OD-EMP-12`) — escalating directly to Admin is an acceptable fallback if that model isn't available at implementation time, not a blocking dependency. | Affects Failure modes/recovery completeness for `REQ-HR-009`. | Product/Human | **RESOLVED — 3-business-day auto-escalation via Outbox, `ADR-041`; reporting-lookup contingent on `GD-03`'s org-chart model, Admin-fallback acceptable** |
| `OD-HR-10` | Open decision | Current-state RBAC (`ROLE_PERMISSIONS` in `backend/src/config/constants.ts`) grants `hr:read`/`hr:write` only to `ADMIN`/`MANAGER`; `WORKER` and `CHECKER` hold no `hr:*` permission. This appears to conflict with CRR §23's explicit "worker can REQUEST a payslip" and the general need for a worker to see their own "contract pending signature" status. **RESOLVED (Option (a)) `GD-15`/`ADR-042`, 2026-07-28:** two new narrow-scope permissions — `hr:payslip:request` (WORKER may create a payslip request for their own `worker_id` only) and `hr:contract:read-own` (WORKER may read their own contract status only) — both self-scoped, server-derived `worker_id`, never client-supplied for Worker-role callers, satisfying `FIND-SEC-HR-03`'s IDOR guard. No blanket `hr:read`/`hr:write` extension to WORKER. Mirrors the platform's existing narrow-capability permission pattern (e.g. `org_chart:read`, `ADR-030`). | Permission matrix above marks the worker-facing rows `[OPEN]`; a worker cannot exercise `REQ-HR-009`'s confirmed capability under current-state RBAC as coded. | Architecture/Human | **RESOLVED — `hr:payslip:request` + `hr:contract:read-own`, both self-scoped, `ADR-042`; `FIND-SEC-HR-03`'s IDOR guard satisfied by construction, not left to implementation discipline** |
| `OD-HR-11` | Open decision | No CRR/PDD retention tier is explicitly assigned to the contract document/PDF/scan itself (only to IBAN/tax ID/payslip-request/wage records, Tier 3). **RESOLVED (provisional) `GD-09`/`ADR-033`, 2026-07-28:** assigned **Tier 3 (6-year payroll/tax-adjacent)** — a signed employment contract is itself a payroll/tax-relevant record under the same statutory basis (§257 HGB/§147 AO) already cited for the IBAN/Tax-ID fields, not merely general personal data. Provisional pending tax-advisor sign-off (`OD-RETENTION-01`). | Affects Data classification/retention completeness. | Product/Human | **RESOLVED (provisional) — Tier 3, `ADR-033`; tax-advisor sign-off tracked separately, non-blocking** |
| `OD-HR-12` | Assumption | Owner unassigned | No `CODEOWNERS`; empty `backend/package.json` author (SYNC-001) | Human | Open — freeze requires a named owner/approver |
| `OD-HR-13` *(v0.2.0, new; `FIND-SEC-HR-04` High)* | Open decision | Current `/hr` routes use only `authMiddleware` + `requirePermission`, never `checkHotelAccess` (confirmed at `backend/src/middleware/permissions.ts` and `backend/src/modules/hr/routes.ts`, current worktree). Once implemented, a Hotel Manager could read/write contract and payroll data — including Tier 3 IBAN/tax ID — for workers at hotels they do not manage. **Write-side PARTIALLY RESOLVED (ADR-030 PR-1):** `POST /contracts`, `POST /payroll`, `POST /workers/:worker_id/documents` now require `requireRole(['admin','manager'])` and a new `checkWorkerScope()` middleware (group-grain, via `EmploymentRecord.hotel_group_id`). `GET /contracts`/`GET /payroll` were narrowed to `requireRole('admin')` rather than scoped (no query-filter schema/data model exists to filter against). **List-route half RESOLVED (Option (a)) `GD-15`/`ADR-043`, 2026-07-28:** `checkWorkerScope()`'s existing group-grain filtering (already built for the write path) extends to `GET /contracts`/`GET /payroll` — both become available to `MANAGER`, server-side filtered to the manager's own `hotel_group_id`; `ADMIN` retains unscoped access. Same mechanism, no new authorization pattern. | Real, previously-unidentified authorization gap (v0.1.0 missed this entirely); high impact given the Tier-3-sensitivity of the data this module touches. | Architecture/Human | **RESOLVED — write-path scoped (`ADR-030` PR-1) and list-route scoped (`ADR-043`) via the same `checkWorkerScope()` mechanism; remaining open: every target-state interface in this spec that has no current route yet** |
| `OD-HR-14` *(v0.2.0, new; `FIND-SEC-HR-06` High)* | Open decision | Malware/content scanning of uploaded contract scans (and other document-class uploads) is not addressed by CRR/PDD. `RULE-HR-13` states content-type/size validation is mandatory but explicitly defers the malware/content-scanning position to this open decision rather than leaving it unstated. **RESOLVED `GD-15`/`ADR-044`, 2026-07-28:** a synchronous malware-scan hook runs at upload time, before the file is persisted — the upload is rejected on detection, never stored. This decision is HR's own; `docs/03-modules/documents/MODULE_SPEC.md`'s mirrored `OD-DOC-016` is a separate, still-open item for that module (verified not itself resolved despite similar framing in that spec's `GD-16` freeze note) — not cited here as settled precedent. Must ship together with `OD-HR-06`'s `Buffer.alloc(0)` fix, per `FIND-SEC-HR-07`. | Affects Failure/Security completeness for the upload endpoint; low-probability but non-trivial risk (malicious file upload) given the endpoint accepts scanned images/PDFs from end users. | Architecture/Human | **RESOLVED — synchronous scan-hook, reject-on-detection, `ADR-044`; ships together with `OD-HR-06`** |
| `OD-HR-15` *(v0.2.0, new; `FIND-SEC-HR-08` High — ESCALATE, disclosure only)* | Open decision | `backend-hr` fully depends on `auth-middleware`/`permissions-middleware` (Dependencies table). `docs/03-modules/auth/MODULE_SPEC.md`'s own independent security review already returned **FAIL** (1 Critical account-takeover finding, 4 High findings) against that module's frozen candidate. **Documentation-synchronization correction (2026-07-30, HR epic acceptance audit — not a new governance decision):** `docs/03-modules/auth/MODULE_SPEC.md`'s own Document Control confirms `SPEC-AUTH-001` was **FROZEN at G2 on 2026-07-15 by the commissioning human**, explicitly "independently of implementation security findings," and that the Critical (`POST /password-reset` account-takeover, `OQ-AUTH-05`) "was already resolved in code by `HOTFIX-AUTH-002`" (verified present in code, `backend/src/config/constants.ts:202-204`). The 4 remaining High findings (`OQ-AUTH-04`/`06`/`15`, absent MFA) were explicitly ruled by that same freeze decision to be **G8 release prerequisites, not G2/freeze blockers** — a disposition already made by human/commissioning authority on the auth side, predating HR's implementation PRs (2026-07-30). This correction narrows, but does not itself resolve, this open decision: the Critical this item was originally raised about is confirmed fixed; the 4 Highs remain open release-track risk that HR's own authorization sits on top of. | `backend-hr` cannot be assessed as independently safe to implement or launch while a Critical remains open in `backend-auth` — every `/hr` route's authorization guarantee is only as strong as the auth layer underneath it. **Current state:** the Critical is fixed; 4 High findings remain open in `backend-auth` as disclosed G8 release-track risk. | Human (explicit Risk Assessment/disposition still required to formally RESOLVE this item — the correction above narrows scope but is not itself that ratification) | **Still Open — narrowed, not resolved.** This spec does not decide whether HR may ship before auth's remaining High findings are dispositioned; a human Risk Assessment or explicit acceptance decision citing this corrected context is required to mark this item RESOLVED. Silent parallel-track approval remains not acceptable. |
| `OD-HR-16` *(v0.2.0, new; `FIND-CONS-HR-02` Medium — disclosure only, does not edit SPEC-EMP-001)* | Open decision | SPEC-EMP-001's own State-and-Lifecycle section for the `Under Review → Active` transition does not list the contract-signed precondition that SPEC-EMP-001's own Dependencies table and PDD §8.1 require (i.e., an employee should not become `Active` without a signed contract, per this module's `RULE-HR-04` gating fact). | Cross-spec inconsistency inside SPEC-EMP-001 itself; this spec's `RULE-HR-04` correctly states it supplies the gating fact, but the consuming spec's own lifecycle narrative does not reflect the precondition. | SPEC-EMP-001 author/reviewers | Open — flagged here for SPEC-EMP-001's own next revision; **not** a defect in or fixable by this spec |

## Proposed Knowledge Deltas

- **`MODULE_REGISTRY.yaml`:** on freeze, set `specification` for `backend-hr` to `SPEC-HR-001`. Do **not** flip `implementation_status` away from its verified `active-no-tests`/stub state, and do **not** change `lifecycle` from `active`, without human confirmation (SYNC-001) — this spec documents target-state behaviour for a module that is presently a stub. **Registry-collision disclosure (`FIND-DEP-HR-03`, Medium — ESCALATE the decision, disclosed here, not resolved):** `MODULE_REGISTRY.yaml`'s single `specification:` field for `backend-hr` (currently `UNKNOWN`, `MODULE_REGISTRY.yaml:150`) is independently claimed by **both** this spec (`SPEC-HR-001`) **and** `docs/03-modules/employee-management/MODULE_SPEC.md`'s own `OD-EMP-10` (which proposes the same `backend-hr` docs↔code mapping for `SPEC-EMP-001`). Both deltas target the same single-valued field. **Both must be jointly dispositioned by a human before either is applied — neither this delta nor SPEC-EMP-001's may silently overwrite the other.** This is the same underlying ambiguity as `OD-HR-01a`, viewed from the registry-schema side.
- **`DEPENDENCY_GRAPH.yaml`:** propose (not apply) the candidate consumer/producer edges implied here — Onboarding → `backend-hr` (Personalfragebogen data read, contract-generation trigger), `backend-hr` → employee-management (contract-signed gating fact), `backend-hr` → Notifications (expiry reminders, payslip-request notifications), `backend-hr` → Retention (Tier 3 classification metadata) — each marked `compatibility: UNKNOWN` (not `conditional`; see `FIND-DEP-HR-01` and the reclassification in the Dependencies table above) until interface schemas resolve (`OD-HR-04`) **and** until the target ids (`Onboarding`, `employee-management`, `Retention`, `Compliance`) are actually registered module ids in `MODULE_REGISTRY.yaml` (none currently are). Flag the missing `frontend-web`/`mobile-checker` consumer edges as an open item rather than adding them speculatively (`OD-HR-05`). Do not alter the `stub_but_registered` entry for `backend-hr` without human confirmation. **Conceptual cycle note (`FIND-DEP-HR-04`, Medium):** adding all three sibling specs' proposed edges together (`backend-hr` → `employee-management` → `Onboarding` → `backend-hr`) creates a latent 3-node conceptual cycle once realized in the graph — flagged for architecture attention at the next dependency-synchronization pass, not resolved here. **External-service gap (`FIND-DEP-HR-05`, Low):** no `external-service` node exists for S3/AWS in `DEPENDENCY_GRAPH.yaml` despite the schema's `kinds:` list supporting that kind (`DEPENDENCY_GRAPH.yaml:16`) — this module's S3 (EU) storage dependency (CRR §9, §25; `RULE-HR-08`) has no corresponding graph node today. **Lifecycle-field disagreement (`FIND-DEP-HR-06`, Low):** `MODULE_REGISTRY.yaml`'s `lifecycle: active` for `backend-hr` (line 145) and `DEPENDENCY_GRAPH.yaml`'s `lifecycle: stub` for `backend-hr` (line 36) already disagree, independent of anything in this spec — flagged, not fixed, by this documentation pass.
- **`TERMINOLOGY.md`:** propose, on human confirmation, canonical terms **Contract status (pending/signed-active/extended/permanent — full four-state set, `FIND-CONS-HR-07`)**, **Manager confirmation (Option A)**, **Familiarization period**, **Payslip request**; note that the documentation module name `hr` (this path) must not be silently merged into or confused with the near-empty `contracts/`, `payslips/`, or `documents/` directories, nor with `employee-management` — see `OD-HR-01a`. **`FIND-CONS-HR-04` note:** the term **Payslip-request processor** is deliberately **not** proposed for unilateral promotion by this spec — its operational-identity meaning (who fulfils) is distinct from the Konfession visibility-policy meaning (which stays SPEC-EMP-001/Compliance-owned); promotion of this term must be coordinated jointly with SPEC-EMP-001, not proposed unilaterally here.
- **`DECISION_INDEX.md`:** register the open decisions `OD-HR-01a`, `OD-HR-01b`, `OD-HR-02`, `OD-HR-02b`, `OD-HR-03`, `OD-HR-05`, `OD-HR-07`, `OD-HR-08`, `OD-HR-09`, `OD-HR-10`, `OD-HR-11`, `OD-HR-13`, `OD-HR-14`, `OD-HR-15`, `OD-HR-16` as pending decision records. `OD-HR-04` is RESOLVED (`ADR-032`, 2026-07-28) and moves out of the pending list. **Schema-gap note (`FIND-CONS-HR-06`, Low):** `DECISION_INDEX.md`'s current schema (see `.claude/knowledge/DECISION_INDEX.md`) has no slot for per-spec `OD-*` tracking — it only tracks top-level `ADR-*` records and an "Unratified Architecture Claims" table. SPEC-EMP-001 proposed the identical delta (a `DECISION_INDEX.md` extension to track its own `OD-EMP-*` items) three days earlier and it was never enacted. This is a systemic gap affecting every module spec that produces Open Decisions, not unique to this spec — but it should not be repeated a third time without acknowledgment; flagged here rather than silently re-proposed as if novel.
- **`SYNC_STATE.yaml`:** record this spec as `REVIEW` pending further G4 independent review and G2 human approval; owner assignment remains blocked (SYNC-001); flag `OD-HR-01a`/`OD-HR-01b` (module-boundary/ownership conflict with the onboarding spec), `OD-HR-10` (RBAC/IDOR gap), `OD-HR-13` (hotel-scoping gap), and `OD-HR-15` (auth-sequencing risk) as requiring cross-spec coordination with SPEC-EMP-001, `docs/03-modules/onboarding/MODULE_SPEC.md`, and `docs/03-modules/auth/MODULE_SPEC.md` respectively.

## Review and Change Log

| Version | Date | Change | Findings resolved | Approver |
|---|---|---|---|---|
| 0.1.0 | 2026-07-08 | Initial canonical-template authoring from CRR/PDD for the `backend-hr` code module (contracts lifecycle, payroll/payslip-request stub, contract-scan document upload). No prior specification exists at this path. Freeze candidate submitted for G4 independent review (architecture, dependency, consistency) and G2 human approval. Author cannot self-approve blocking findings (Constitution §12); **FROZEN status is withheld pending human approval and disposition of open decisions `OD-HR-01..12`, most notably the docs↔docs module-boundary question (`OD-HR-01`) and the current-state RBAC/requirement conflict (`OD-HR-10`).** | — (none dispositioned yet) | — (pending) |
| 0.2.0 | 2026-07-08 | Correction pass applying G4 independent review findings against v0.1.0 (rev `7a062d2`): Architecture=`BLOCKED`, Dependency=`PASS_WITH_ACTIONS`, Consistency=`FAIL`, Security=`FAIL`, Performance=`PASS_WITH_ACTIONS`. **Key new evidence:** `docs/03-modules/onboarding/MODULE_SPEC.md` (never cited in v0.1.0) independently claims ownership of contract generation and manager-confirmation capture, directly conflicting with this spec's `RULE-HR-01`/`REQ-HR-001` and `RULE-HR-03`/`REQ-HR-03` — disclosed, not resolved, as `OD-HR-01a` (expanded from `OD-HR-01`) plus a new `OD-HR-01b` decision-record requirement. Applied all author-fixable corrections: expanded Evidence/Traceability and Out-of-Scope citations for the onboarding conflict; added `OD-HR-02b` (Personalfragebogen data-source ambiguity, left genuinely unresolved); reclassified five Dependencies-table edges (Onboarding, employee-management, Documents, Retention, Compliance) from `compatibility: conditional` to `UNKNOWN` and reworded the Documents row as an intra-module mechanism, not a cross-module edge; added a conceptual dependency-cycle note; added S3 external-service-node and registry lifecycle-mismatch notes to Proposed Knowledge Deltas; added a registry-collision disclosure (`backend-hr`'s single `specification:` field independently claimed by this spec and SPEC-EMP-001's `OD-EMP-10` — jointly dispositioned by a human, not silently overwritten); added new normative `REQ-HR-013`/`RULE-HR-15` (concrete immutable audit-event schema for manager-confirmation: actor id, timestamp, worker id, contract id, evidence-file reference), `REQ-HR-014`/`RULE-HR-14` (manager identity MUST derive from `req.auth`, never client-supplied), and `REQ-HR-015`/`RULE-HR-13` (file-upload content-type/size validation, server-generated storage keys, explicit deferred-malware-scan position tracked as new `OD-HR-14`); strengthened `OD-HR-10` with explicit IDOR-scoping guidance for `IF-HR-RequestPayslip`/`IF-HR-GetContractStatus`; linked `OD-HR-06` to the new upload-validation rule so they ship together; added new `OD-HR-13` (hotel-scoping gap — `/hr` routes never call `checkHotelAccess`, confirmed at `backend/src/modules/hr/routes.ts` and `backend/src/middleware/permissions.ts`); added new `OD-HR-15` (disclosure-only: `backend-hr` depends on `auth-middleware`, and `docs/03-modules/auth/MODULE_SPEC.md`'s own security review returned FAIL with 1 Critical/4 High — shipping HR before that Critical is fixed requires an explicit human Risk Assessment); added new `OD-HR-16` (disclosure-only cross-spec note: SPEC-EMP-001's own `Under Review → Active` lifecycle section omits the contract-signed precondition its own Dependencies table/PDD §8.1 require — flagged for SPEC-EMP-001's next revision, not fixed here); split the merged "Hotel Manager / Regional Manager" terminology row into two, left the "Admin" drift as a noted-but-accepted tracked item, clarified the "Payslip-request processor" row as operational-identity-only (coordination with SPEC-EMP-001 required before any terminology promotion), and expanded the "Contract status" row to the full four-state set; added a `DECISION_INDEX.md` schema-gap note (no per-spec `OD-*` tracking slot exists; SPEC-EMP-001 proposed the same delta three days earlier, never enacted). Findings addressed: `FIND-ARCH-HR-01`, `FIND-ARCH-HR-02`, `FIND-DEP-HR-01`, `FIND-DEP-HR-02`, `FIND-DEP-HR-03`, `FIND-DEP-HR-04`, `FIND-DEP-HR-05`, `FIND-DEP-HR-06`, `FIND-CONS-HR-01`, `FIND-CONS-HR-02`, `FIND-CONS-HR-03`, `FIND-CONS-HR-04`, `FIND-CONS-HR-05`, `FIND-CONS-HR-06`, `FIND-CONS-HR-07`, `FIND-SEC-HR-01`, `FIND-SEC-HR-02`, `FIND-SEC-HR-03`, `FIND-SEC-HR-04`, `FIND-SEC-HR-06`, `FIND-SEC-HR-07`, `FIND-SEC-HR-08`; all Performance `PASS_WITH_ACTIONS`-level notes acknowledged as non-blocking for this pass (no Performance findings required a spec-text correction). **`OD-HR-01a`/`OD-HR-01b` (module-boundary/ownership question, now BLOCKING) and `OD-HR-15` (auth-sequencing risk) remain unresolved pending human/architecture decision. FROZEN status continues to be withheld — this document remains `REVIEW`.** | `FIND-ARCH-HR-01`, `FIND-ARCH-HR-02`, `FIND-DEP-HR-01..06`, `FIND-CONS-HR-01..07`, `FIND-SEC-HR-01,02,03,04,06,07,08` (all disclosed/corrected, none silently resolved where escalation-only) | — (pending) |
| 0.2.1 | 2026-07-08 | Bounded re-review of v0.2.0 (commit `d174150`) by architecture, consistency, and security reviewers, verifying disposition of their own prior findings rather than a full re-derivation. Results: Architecture re-confirms `BLOCKED` — disclosure of `OD-HR-01a`/`OD-HR-01b` is now adequate, but the module-boundary conflict with `docs/03-modules/onboarding/MODULE_SPEC.md` remains a genuine, unresolved ownership ambiguity requiring a human-ratified Decision Record; this is a "clean" `BLOCKED` per `LOOP_CONTROL.md` §4, not an author defect, and further correction cycles cannot move it. Architecture also raised one new Low, non-blocking observation (`FIND-ARCH-HR-06`: the future `checkHotelAccess` coupling implied by `OD-HR-13` is not yet modeled as a dependency-graph edge). Consistency moved to `PASS_WITH_ACTIONS` (`FIND-CONS-HR-01` through `07` all correctly dispositioned) but found one new Low defect: two off-by-one finding-ID citation typos in the Actors and Terminology table (Admin row cited `FIND-CONS-HR-03` instead of `-04`; Payslip-request-processor row cited `-04` instead of `-05`) — both corrected in this version. Security moved to `PASS_WITH_ACTIONS` (`FIND-SEC-HR-01` through `08` all correctly dispositioned, each with concrete MUST-level text verified against the actual document wording) but found one new Medium gap: the Validation Plan table was never extended for the three new v0.2.0 requirements (`REQ-HR-013`/`014`/`015`) — three rows added in this version — and one new Low completeness gap: `OD-HR-13`'s hotel-scoping cross-reference was present on only 3 of 7 relevant interface rows — extended to all 7 (`IF-HR-ListContracts`, `IF-HR-CreateContract`, `IF-HR-ListPayroll`, `IF-HR-CreatePayroll` now also cross-reference `OD-HR-13`) in this version. Security's required action to obtain a human-authorized Risk Assessment for `OD-HR-15` before implementation is not resolvable by this document and remains open. **This spec's overall G4 disposition is `BLOCKED` on the architecture component alone; dependency and consistency and security and performance components are all `PASS`/`PASS_WITH_ACTIONS`. Per Constitution §12/§18 and `REVIEW_GATES.md`, a `BLOCKED` component blocks G2 freeze and further workflow advancement (e.g., to Documentation Validator/G6-doc) until resolved by human/architecture authority — this document does not proceed past this gate on its own. FROZEN status continues to be withheld; escalation to the human is the correct next step, per the accompanying Lead Architect escalation.** | `FIND-CONS-HR-*` citation typos; `REQ-HR-013/014/015` Validation Plan coverage; `OD-HR-13` cross-reference completeness (`FIND-ARCH-HR-06` recorded as open, non-blocking) | — (pending; architecture `BLOCKED` requires human ADR before any further gate progress) |
| 0.2.1 (byproduct correction, recorded not versioned) | 2026-07-25 | Narrow correction applied as a byproduct of `ADR-030` PR-1 (Manager Write Authority security hardening) — not a standalone documentation pass. `OD-HR-13`'s current-state row, the Hotel-scoping-gap paragraph, the Authentication/Authorization dependency row, and the permission-matrix table (Trust boundaries/authorization section) all updated to reflect that `checkWorkerScope()` (`backend/src/middleware/permissions.ts`, backed by `isWorkerInGroupScope()` in `backend/src/lib/scope.ts`) now scopes the three worker_id-carrying write routes, and that the two list routes were narrowed to Admin-only rather than scoped. `OD-HR-13` remains open (list-route scope-filtering and every not-yet-built target-state interface); `OD-HR-01a`/`OD-HR-01b` (module-boundary, architecture `BLOCKED`) and `OD-HR-15` are entirely unaffected and remain open. No version bump (nonsemantic correction, `LOOP_CONTROL.md` §7 exemption, mirroring this document's own citation-fix precedent at v0.2.1 above). | `OD-HR-13` (partially — write-path only) | — (byproduct correction; no new G4 round) |
| 0.2.1 (byproduct correction, recorded not versioned) | 2026-07-28 | Narrow correction applied as a byproduct of `GD-12`'s ratification (`ADR-032`, Platform event-bus / inter-module transport, Decided via the Governance Resolution workflow) — not a standalone documentation pass. `OD-HR-04` (formal domain-event/interface contract) marked RESOLVED at the transport-convention level: direct in-process call for synchronous effects, the existing Outbox for asynchronous/durable effects, no generic event bus — matching `SPEC-EMP-001`'s identical `OD-EMP-09` resolution the same day. This resolves the platform-level transport question only; it does **not** unblock this module's own build, which remains gated on `OD-HR-01a`/`OD-HR-01b` (architecture `BLOCKED`, module-boundary dispute with `docs/03-modules/onboarding/MODULE_SPEC.md`) and `GD-15` (HR & Employee-Management module build scope, still open). No version bump (nonsemantic correction, `LOOP_CONTROL.md` §7 exemption, mirroring this document's own `ADR-030` byproduct-correction precedent above). | `OD-HR-04` RESOLVED (`ADR-032`) | — (byproduct correction; no new G4 round; architecture `BLOCKED` on `OD-HR-01a` unaffected) |
| 0.2.1 (byproduct correction, recorded not versioned) | 2026-07-28 | Narrow correction applied as a byproduct of `GD-09`'s ratification (`ADR-033`, GDPR retention-tier assignment, Decided via the Governance Resolution workflow, Option (c): provisional mapping now, tax-advisor sign-off tracked separately). `OD-HR-11` (contract document retention tier) marked RESOLVED (provisional) — assigned Tier 3 (6-year payroll/tax-adjacent), pending tax-advisor sign-off (`OD-RETENTION-01`), non-blocking. This does **not** unblock this module's own build, which remains gated on `OD-HR-01a`/`OD-HR-01b` (architecture `BLOCKED`) and `GD-15`. No version bump (nonsemantic correction, `LOOP_CONTROL.md` §7 exemption). | `OD-HR-11` RESOLVED (provisional, `ADR-033`) | — (byproduct correction; no new G4 round; architecture `BLOCKED` on `OD-HR-01a` unaffected) |
| 0.2.2 | 2026-07-28 | **Documentation-synchronization correction, discovered while auditing `GD-15` (HR & Employee-Management module build scope) before presenting it to the commissioning human — not a new governance decision.** `OD-HR-01a`/`OD-HR-01b` (module-boundary dispute with `docs/03-modules/onboarding/MODULE_SPEC.md` over contract generation/manager-confirmation-capture ownership, standing architecture `BLOCKED` since v0.2.0) were found to have **already been resolved by `ADR-012` (Accepted, 2026-07-12)** — `backend-hr`/`SPEC-HR-001` is the ratified canonical owner; Onboarding's overlapping first-person ownership claim (§5 items 4-5, §6.5, §7 steps 7-8) is superseded, not authoritative. This document's own text (Status field, v0.2.0 correction note, `OD-HR-01a`/`OD-HR-01b` rows, and every subsequent byproduct-correction row through 2026-07-28) was never updated to cite `ADR-012` — the conflict was never live after 2026-07-12, only undocumented as resolved. Corrected in place: Status field, v0.2.0 note, and both rows now marked RESOLVED with `ADR-012` citation. The corresponding correction was also applied to `docs/03-modules/onboarding/MODULE_SPEC.md` (§5 items 4-5, §6.5, §7 steps 7-8) in the same pass, mirroring `ADR-013`'s already-completed Chatbot correction to that same document. Architecture status upgraded from `BLOCKED` to no-longer-blocked on this item; this module's own G2 freeze remains a separate, still-outstanding step pending its other open items (`OD-HR-02/03/07/09/10/14/15`, ownership assignment). | `OD-HR-01a`, `OD-HR-01b` RESOLVED (`ADR-012`) | Lead Architect (2026-07-28, documentation-synchronization correction, discovered during `GD-15` audit) |
| 0.2.3 | 2026-07-28 | **`GD-15` sub-decision 1 of 10, per `ADR-039`** (Payroll model conflict, Decided via the Governance Resolution workflow, Option (a)). `OD-HR-02` resolved: target-state types redesigned to eliminate the conflict with CRR §23's zero-payroll-math non-goal. `CreateContractRequest` drops `salary_amount` (contract generation is document production, not compensation-setting). `CreatePayrollRequest`/`IF-HR-CreatePayroll` becomes a pure payslip-request record (`worker_id`, `period_start`, `period_end`, `status`) — no gross-salary or computation field of any kind; actual wage calculation happens outside this system. `IF-HR-ListPayroll` lists these request records, not computed payroll data. No code changes (all affected routes remain 501 Not Implemented); this settles the target shape only. Nine `GD-15` sub-decisions remain open (`OD-HR-03/07/09/10/13/14` and `OD-EMP-04/06/08/13/14`). | `OD-HR-02` RESOLVED (`ADR-039`) | Commissioning human (2026-07-28, Governance Resolution workflow, `GD-15` sub-decision 1/10) |
| 0.2.4 | 2026-07-28 | **`GD-15` sub-decision 2 of 10, per `ADR-040`** (Contract lapse & continuation-capture mechanism, Decided via the Governance Resolution workflow, Option (a)). `OD-HR-03` and `OD-HR-07` resolved: continuation/permanence confirmation at the 1-year and 2-year marks is manager-only — a single manager action confirms the transition; no worker-side approval/veto step. Lapse is triggered by an explicit manager "do not continue" action or manager silence past a deadline, which triggers offboarding via `SPEC-EMP-001`'s own `OD-EMP-04` (itself still open, tracked separately). `RULE-HR-06`'s exception-handling text updated accordingly. No code changes. Eight `GD-15` sub-decisions remain open (`OD-HR-09/10/13/14` and `OD-EMP-04/06/08/13/14`). | `OD-HR-03`, `OD-HR-07` RESOLVED (`ADR-040`) | Commissioning human (2026-07-28, Governance Resolution workflow, `GD-15` sub-decision 2/10) |
| 0.2.5 | 2026-07-28 | **`GD-15` sub-decision 3 of 10, per `ADR-041`** (Payslip-request escalation, Decided via the Governance Resolution workflow, Option (a), 3-business-day window). `OD-HR-09` resolved: an unfulfilled payslip request auto-escalates after 3 business days (configurable) to a notification via the existing Outbox (`ADR-029`), sent to the requesting worker's manager's own manager, or Admin as a fallback if no reporting-structure manager exists (the manager-lookup depends on `GD-03`'s still-open org-chart data model, `OD-EMP-12` — Admin-fallback is acceptable, not blocking). Escalation is notification-only, not re-assignment or auto-fulfilment. No code changes. Seven `GD-15` sub-decisions remain open (`OD-HR-10/13/14` and `OD-EMP-04/06/08/13/14`). | `OD-HR-09` RESOLVED (`ADR-041`) | Commissioning human (2026-07-28, Governance Resolution workflow, `GD-15` sub-decision 3/10) |
| 0.2.6 | 2026-07-28 | **`GD-15` sub-decision 4 of 10, per `ADR-042`** (Worker-facing RBAC, Decided via the Governance Resolution workflow, Option (a)). `OD-HR-10` resolved: two new narrow, self-scoped permissions — `hr:payslip:request` and `hr:contract:read-own` — grant `WORKER` access to their own payslip-request/contract-status data only, with `worker_id` server-derived from the authenticated caller's identity (never client-supplied for Worker-role callers), directly satisfying `FIND-SEC-HR-03`'s IDOR-prevention requirement. No blanket `hr:read`/`hr:write` extension to `WORKER`; `ADMIN`/`MANAGER`'s existing scope unchanged. Mirrors the platform's narrow-permission pattern (`org_chart:read`, `ADR-030`). No code changes. Six `GD-15` sub-decisions remain open (`OD-HR-13/14` and `OD-EMP-04/06/08/13/14`). | `OD-HR-10` RESOLVED (`ADR-042`) | Commissioning human (2026-07-28, Governance Resolution workflow, `GD-15` sub-decision 4/10) |
| 0.2.7 | 2026-07-28 | **`GD-15` sub-decision 5 of 10, per `ADR-043`** (List-route hotel-scope filtering, Decided via the Governance Resolution workflow, Option (a)). `OD-HR-13`'s remaining list-route half resolved: `checkWorkerScope()`'s existing group-grain filtering (already shipped for the write path via `ADR-030` PR-1) extends to `GET /contracts`/`GET /payroll` — both become available to `MANAGER`, server-side filtered to the manager's own `hotel_group_id`; `ADMIN` retains unscoped access. Same mechanism, no new authorization pattern; the original blocker (no query-filter schema) was resolved by `ADR-039`'s target-type redesign. `OD-HR-13` is now fully resolved (write-path + list-route, both via `checkWorkerScope()`); its only remaining caveat (every not-yet-built target-state interface) is unaffected. No code changes. Five `GD-15` sub-decisions remain open (`OD-HR-14` and `OD-EMP-04/06/08/13/14`). | `OD-HR-13` RESOLVED (`ADR-043`) | Commissioning human (2026-07-28, Governance Resolution workflow, `GD-15` sub-decision 5/10) |
| 0.2.8 | 2026-07-28 | **`GD-15` sub-decision 6 of 10, per `ADR-044`** (Malware-scan position for contract-scan uploads, Decided via the Governance Resolution workflow). `OD-HR-14` resolved: a synchronous malware-scan hook runs at upload time, before the file is persisted; the upload is rejected on detection, never stored. Verification found `docs/03-modules/documents/MODULE_SPEC.md`'s structurally identical `OD-DOC-016` is itself still an open, unresolved item (not the settled precedent its own `GD-16` freeze note implied) — this decision was made independently for HR, not by citing that as prior art. Must ship together with `OD-HR-06`'s `Buffer.alloc(0)` fix, per `FIND-SEC-HR-07` — `RULE-HR-13` and `OD-HR-06` updated to cross-reference the resolved position. No code changes. Four `GD-15` sub-decisions remain open (`OD-EMP-04/06/08/13/14`). | `OD-HR-14` RESOLVED (`ADR-044`) | Commissioning human (2026-07-28, Governance Resolution workflow, `GD-15` sub-decision 6/10) |
| 0.2.9 | 2026-07-28 | **Cross-reference update, byproduct of `GD-15` sub-decision 7, per `ADR-045`** (Employee-Management offboarding trigger, Decided via the Governance Resolution workflow, Option (c) hybrid). `OD-HR-03`'s downstream reference to `SPEC-EMP-001`'s `OD-EMP-04` is updated: that item is now resolved (`ADR-045`) — contract lapse (this spec's own `OD-HR-03`/`ADR-040`) triggers `employee-management`'s `Deactivated` transition automatically via a direct in-process call (`ADR-032`). The contract-lapse-to-offboarding mechanism is now resolved end-to-end across both specs. No other requirement, rule, or open decision touched in this document. | `OD-HR-03` cross-reference updated (`ADR-045`) | Commissioning human (2026-07-28, Governance Resolution workflow, `GD-15` sub-decision 7/10, HR-side cross-reference) |
| 0.2.9 (byproduct correction, recorded not versioned) | 2026-07-30 | **Documentation-synchronization correction, discovered during the HR epic final acceptance audit — not a new governance decision.** `OD-HR-15` (auth-dependency sequencing risk, `FIND-SEC-HR-08`) was raised against `docs/03-modules/auth/MODULE_SPEC.md`'s security review, which returned FAIL (1 Critical, 4 High) at the time this item was written. Re-verified against that document's current Document Control: `SPEC-AUTH-001` was FROZEN at G2 on 2026-07-15 by the commissioning human, explicitly independent of the security findings, and its Critical (`OQ-AUTH-05`, password-reset account-takeover) is recorded there as already resolved in code by `HOTFIX-AUTH-002` (confirmed present, `backend/src/config/constants.ts:202-204`) — both facts predate HR's own implementation PRs (2026-07-30). `OD-HR-15`'s row is corrected to reflect this: the Critical it was raised about is fixed; the 4 remaining High findings in `backend-auth` were separately ruled by that same freeze decision to be G8 release prerequisites rather than freeze blockers, and remain open. **This correction narrows `OD-HR-15`'s stated risk; it does not itself constitute the human Risk Assessment/disposition the item's own text requires to be marked RESOLVED — that ratification remains an outstanding human action.** No requirement, rule, or code touched by this correction. | `OD-HR-15` context corrected (not resolved) | Lead Architect (2026-07-30, documentation-synchronization correction, discovered during HR epic acceptance audit) |
| 0.2.10 (byproduct correction, recorded not versioned) | 2026-08-05 | **Documentation-synchronization correction, Regional Manager V1 (PR #338) — not a new governance decision.** The Permission-matrix header's "current-state code only distinguishes `ADMIN`/`MANAGER` vs. others for `hr:*` — Regional Manager scope narrowing... is not separately modeled in code today" is now stale: `hr/routes.ts` gates all 11 admin/manager-class routes on `requireRole([...,'regional_manager'])`, and `middleware/permissions.ts#resolveWorkerScope()` special-cases `regional_manager` alongside `manager` via `isScopedManagerRole()` (closing the specific gap this header described — RM previously held `hr:read`/`hr:write` via `ROLE_PERMISSIONS` but was denied at both the role gate and the worker-scope seam). This also corrects the two "list routes were narrowed to Admin only" statements immediately above and in the matrix rows below: `GET /contracts` and `GET /payroll` (via `requirePayslipReadAccess()`) both admit `regional_manager`, group-scoped through the same `resolveWorkerScope()`/`isWorkerInGroupScope()` seam as the write routes — list-route scope-FILTERING (as opposed to role admission) remains the genuinely open half of `OD-HR-13` this document already correctly disclaims. The matrix rows marked "intent only, no current route, `OD-HR-13`" for the Hotel Manager/Regional Manager columns should be read as "intent, PARTIALLY enforced" wherever a route now exists gating on `regional_manager` — a full per-row resweep against current `hr/routes.ts` is recommended at the next HR-module-specific documentation pass rather than hand-verifying all ~15 matrix rows in this cross-module synchronization pass. No requirement, rule, or code touched by this correction. | Permission-matrix header and its two immediately-following "list routes... Admin only" claims corrected; per-row resweep recommended, not performed here | Lead Architect (2026-08-05, Regional Manager V1 PR6 documentation synchronization) |

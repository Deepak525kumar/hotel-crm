schema_version: 1
purpose: >-
  Canonical lookup: state domain → authoritative writer, all writers, and readers
  (framework 1.2.0). Derived from DEPENDENCY_GRAPH.yaml state_domains; summarizes it
  and never outranks it (knowledge/README.md Rule 3). Consumed by boundary-collision
  (G1.5) to detect cross-module state-write collisions and by architecture review.
  Designating a single authoritative writer where UNKNOWN remains reserved human
  authority (Constitution §7; SYNC-005).
scope: project-runtime
derived_from: [.claude/knowledge/DEPENDENCY_GRAPH.yaml]
version_source: .claude/VERSION.yaml
observed_revision: 0169ecb  # rebound to HEAD 0169ecb by SYNC-072 (Consent repository synchronization pass, 2026-07-31) -- state-consent-record/ConsentRecord row added (was entirely absent), single-writer backend-consent. Prior: rebound to HEAD 1aa5f88bce9ef27c9b0563363ebaa211d8aca240 by SYNC-055 (post-PR#243/#244/#245 repository-synchronization pass, 2026-07-28); content verified coherent 3254c83..1aa5f88 -- GD-18 calendar-absence narrow slice (PR #243), GD-16 decision + SPEC-DOCUMENTS-001 freeze (PR #244), backend-documents module implementation (PR #245) now reflected in-content (state-calendar-absence and state-worker-document nodes added, backend-documents module node added, stale backend-calendar orphan/stub notes corrected). Prior: rebound to HEAD 3254c83754f3660dee968c5138a32b6bc54d5578 by SYNC-054 (post-ADR-030 PR-1..PR-8 repository-synchronization pass, 2026-07-26); content verified coherent 9ca0eed..3254c83 -- ADR-030 capability-model rollout (REGIONAL_MANAGER role, capability-named gates replacing ManagerAdminGate, resolveHotelAccess/analytics regional_manager fixes) now reflected in permissions-middleware notes. Prior: rebound to HEAD 9ca0eed by SYNC-053 (post-#194 repository-synchronization + planning pass, 2026-07-23); content verified coherent 09e0b16..9ca0eed -- Epic 5/6/7/8 already reflected in-content, 367/367 backend tests green, tsc clean, repository-integrity-check.js exit 0. Prior: rebound by Repository Synchronization Workflow (Revision Rebind mode, SYNC-037) -- stamp-only, no content change; prior stamp 39fc1f5 (Package A, AUDIT-REPO-2026-07-14, stamp-only: content already reflected ADR-016 state-audit-log authoritative_writer: backend-auth, the source of truth OWNERSHIP_INDEX.yaml/BOUNDARY_INDEX.yaml were re-propagated to match, AUDIT-H1) was two merges behind HEAD; content verified unchanged 39fc1f5..09e0b16

state_domains:
  - { domain: state-user, model: User, authoritative_writer: backend-auth, writers: [backend-auth, backend-users], readers: [], note: "authoritative_writer resolved backend-auth by ADR-017 (Accepted, 2026-07-15); backend-users is a bounded profile writer/reader (non-security fields only); role/permission/credential/account writes exclusively backend-auth; existing call sites unchanged by this record -- closes SYNC-005/SIR-GLOB-006's state-user half (state-audit-log half already closed by ADR-016). backend-hotel-workers removed from readers by Epic 5 PR 5.8 (module physically deleted); mirrors DEPENDENCY_GRAPH.yaml:523." }
  - { domain: state-session, model: Session, authoritative_writer: backend-auth, writers: [backend-auth], readers: [backend-auth] }
  - { domain: state-hotel, model: Hotel, authoritative_writer: backend-crm, writers: [backend-crm], readers: [backend-job-requests, backend-geo], note: "backend-hotel-workers removed from readers by Epic 5 PR 5.8 (module physically deleted); backend-work-requests renamed backend-job-requests by Epic 9 PR 9.4 (TREQ-013/MIG-GAP-12, See SYNC-061); mirrors DEPENDENCY_GRAPH.yaml (authoritative source). backend-geo added as a reader (2026-07-30, Geo repository synchronization pass): geo/service.ts:26,48 reads Hotel.latitude/longitude directly via Prisma (OD-GEO-001, GD-14) -- a direct cross-module state read, not a service-to-service call; was missing from this row despite existing since the Geo implementation landed 2026-07-28." }
  - { domain: state-hotel-worker, model: HotelWorker, authoritative_writer: none, writers: [], readers: [], note: "DORMANT since Epic 5 PR 5.8 (ADR-022 roadmap step 6, ADR-024 D5): backend-hotel-workers (the CRUD module and sole writer) was physically deleted; PR 5.7 repointed all six former consumer modules (backend-work-requests [renamed backend-job-requests by Epic 9 PR 9.4, see SYNC-061], backend-work-applications, backend-assignments, backend-users, backend-analytics, backend-quality) to state-employment-record via lib/roster-scope.ts, and PR 5.8 removed the FEATURE_ROSTER_CUTOVER flag, making that the sole path. CODE-ONLY retirement per explicit human decision (2026-07-22): the HotelWorker table/model itself is deliberately untouched in schema.prisma -- no migration, no DROP TABLE, no data loss -- per ADR-022's Data Migration Plan ('No DROP TABLE'); no backfill of HotelWorker rows into EmploymentRecord has ever been built or run. `npm run employment-record:backfill-status` reports any ACTIVE HotelWorker rows with no corresponding EmploymentRecord, read-only, for a future human decision on the table's ultimate fate. Mirrors DEPENDENCY_GRAPH.yaml:539,543 (authoritative source)." }
  - { domain: state-job-request, model: JobRequest, authoritative_writer: backend-job-requests, writers: [backend-job-requests], readers: [backend-analytics, backend-assignments], note: "Renamed from state-work-request (owner backend-work-requests) by Epic 9 PR 9.4 (TREQ-013/MIG-GAP-12): Prisma-level model rename only, @@map(\"WorkRequest\") preserves the physical table name, zero data migration. backend-work-applications removed from writers/readers by Epic 9 PR 9.2 (commits 12b1c31/2e89c4f, merged b035700; TREQ-011/ADR-058) -- the module was physically deleted. backend-assignments added as a reader by Epic 9 PR 9.3 (WorkerAssignment.job_request_id FK, schema-only-unpopulated; see SYNC-060). The historical cross-module write coupling (SUPERSEDED-BY-PIVOT per ADR-018, Accepted 2026-07-15) is recorded in git history; see SYNC-059, SYNC-061. Mirrors DEPENDENCY_GRAPH.yaml (authoritative source)." }
  # state-work-application row REMOVED by Epic 9 PR 9.2 (commits 12b1c31/2e89c4f,
  # merged b035700; TREQ-011/ADR-058) -- the WorkApplication model was dropped from
  # schema.prisma (migration 20260729000000_drop_work_application) and its sole
  # writer, backend-work-applications, was physically deleted. No row remains; see
  # git history for the historical record and DEPENDENCY_GRAPH.yaml (authoritative
  # source) for the full retirement note. See SYNC-059.
  - { domain: state-worker-assignment, model: WorkerAssignment, authoritative_writer: backend-assignments, writers: [backend-assignments], readers: [backend-quality, backend-attendance, backend-analytics], note: "backend-work-applications removed from writers by Epic 9 PR 9.2 (commits 12b1c31/2e89c4f, merged b035700; TREQ-011/ADR-058) -- the module was physically deleted; it no longer creates WorkerAssignment records. The historical collision (record created by work-applications in accept tx, owned/updated by assignments) is recorded in git history; see SYNC-059." }
  - { domain: state-attendance, model: Attendance, authoritative_writer: backend-attendance, writers: [backend-attendance], readers: [backend-analytics], note: "backend-work-applications removed from writers by Epic 9 PR 9.2 (commits 12b1c31/2e89c4f, merged b035700; TREQ-011/ADR-058) -- the module was physically deleted; it no longer seeds Attendance rows. The historical collision (seeded by work-applications in accept tx) is recorded in git history; see SYNC-059." }
  - { domain: state-quality-verification, model: QualityVerification, authoritative_writer: backend-quality, writers: [backend-quality], readers: [backend-analytics] }
  - { domain: state-rating, model: Rating, authoritative_writer: backend-quality, writers: [backend-quality], readers: [backend-analytics] }
  - { domain: state-worker-overall-rating, model: WorkerOverallRating, authoritative_writer: backend-quality, writers: [backend-quality], readers: [backend-analytics], note: "backend-work-applications removed from readers by Epic 9 PR 9.2 (commits 12b1c31/2e89c4f, merged b035700; TREQ-011/ADR-058) -- the module was physically deleted. See SYNC-059." }
  - { domain: state-rooms-completed-entry, model: RoomsCompletedEntry, authoritative_writer: backend-assignments, writers: [backend-assignments], readers: [backend-analytics], note: "added by ADR-028 (2026-07-22, OQ-ANALYTICS-03); manager-entered 'rooms completed' daily count, 1-to-1 with WorkerAssignment, no room-level task layer per CONFIRMED §33; single-writer, no collision" }
  - { domain: state-notification, model: Notification, authoritative_writer: backend-notifications, writers: [backend-notifications], readers: [backend-notifications] }
  - { domain: state-audit-log, model: AuditLog, authoritative_writer: backend-auth, writers: [shared-via-base-service], readers: [backend-compliance], note: "authoritative_writer resolved backend-auth by ADR-016 (Accepted, 2026-07-14); existing writers continue writing via the shared BaseService.logAudit helper unchanged (no refactor performed by this record); backend-compliance is a read-only consumer of AuditLog, never a writer, per ADR-016 -- closes SYNC-005/SIR-GLOB-006's state-audit-log half. state-user's dual-writer collision (below) remains unresolved." }
  - { domain: state-password-reset-token, model: PasswordResetToken, authoritative_writer: backend-auth, writers: [backend-auth], readers: [backend-auth], note: "added by HOTFIX-AUTH-002 (SIR-AUTH-001); single-writer, no collision" }
  - { domain: state-hr-contract, model: Contract, authoritative_writer: backend-hr, writers: [backend-hr], readers: [backend-hr], note: "Added by HR implementation PR 1 (migration 20260730010000_add_hr_contract_payslip_request, ADR-012). backend-hr is the sole reader/writer -- no other module reads or writes Contract directly; employee-management's account-activation gating fact and contract-lapse deactivation are triggered via direct in-process service calls (deactivateForContractLapse()), not by reading/writing this table. Missing from this index prior to the HR epic post-closeout synchronization pass (2026-07-30); mirrors DEPENDENCY_GRAPH.yaml (authoritative source)." }
  - { domain: state-hr-payslip-request, model: PayslipRequest, authoritative_writer: backend-hr, writers: [backend-hr], readers: [backend-hr], note: "Added by HR implementation PR 1 (migration 20260730010000_add_hr_contract_payslip_request, ADR-014). backend-hr is the sole reader/writer of PayslipRequest; single-writer, no collision. Missing from this index prior to the HR epic post-closeout synchronization pass (2026-07-30); mirrors DEPENDENCY_GRAPH.yaml (authoritative source)." }
  - { domain: state-geo-checkin, model: WorkerGeoCheckin, authoritative_writer: backend-geo, writers: [backend-geo], readers: [backend-geo], note: "Added by Geo implementation (migration 20260728010000_add_worker_geo_checkin, GD-14/OD-GEO-002, 2026-07-28). backend-geo is the sole reader/writer -- backend-attendance never writes this table directly, it only consumes GeoService.verifyGeofence()/isGeofenceConfigured() in-process and branches on the returned GeofenceVerification result (attendance/service.ts:65-85). GDPR Tier 1 (CRR §25); GeoRetentionSweepJob hard-deletes rows older than 6 months (TREQ-GEO-005/RULE-GEO-004). Missing from this index prior to the Geo repository synchronization pass (2026-07-30); mirrors DEPENDENCY_GRAPH.yaml (authoritative source)." }
  - { domain: state-consent-record, model: ConsentRecord, authoritative_writer: backend-consent, writers: [backend-consent], readers: [backend-consent], note: "Added by Consent implementation (PR #293, migration 20260731000000_add_consent_record, GD-17/ADR-037, 2026-07-31). backend-consent is the sole reader/writer -- every decision is a new INSERT (append-only, RULE-CONSENT-05, no update/delete), the same immutability shape state-geo-checkin/WorkerGeoCheckin already establishes. GDPR retention Tier 2 (provisional, OD-CONSENT-003/ADR-033), pending tax-advisor sign-off (OD-RETENTION-01) -- no sweep job exists yet for this table (unlike WorkerGeoCheckin's GeoRetentionSweepJob), a disclosed, non-blocking gap. Missing from this index prior to the Consent repository synchronization pass (2026-07-31); mirrors DEPENDENCY_GRAPH.yaml (authoritative source)." }

# cross_module_write_couplings: G1.5 must treat these as ownership-bearing. The
# backend-work-applications coupling (wrote state-work-request,
# state-worker-assignment, state-attendance during accept-application
# transaction) no longer exists: the module was physically deleted by Epic 9
# PR 9.2 (commits 12b1c31/2e89c4f, merged b035700; TREQ-011/ADR-058). See git
# history for the historical record and SYNC-059.
cross_module_write_couplings: []

invalidated_by: [schema change, ownership/state decision, DEPENDENCY_GRAPH revision change]

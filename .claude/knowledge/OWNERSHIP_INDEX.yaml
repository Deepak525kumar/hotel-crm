schema_version: 1
purpose: >-
  Canonical lookup: accountable owner per module, shared contract, and state
  domain (framework 1.2.0). Derived from MODULE_REGISTRY.yaml + DEPENDENCY_GRAPH.yaml;
  summarizes them and never outranks them (knowledge/README.md Rule 3). Consumed by
  boundary-collision (G1.5) to detect ownership collisions without re-reading the
  registry/graph. `unassigned` is an evidence-backed value (no CODEOWNERS; empty
  package.json author), never an invented owner (SYNC-001, blocked, human authority).
scope: project-runtime
derived_from: [.claude/knowledge/MODULE_REGISTRY.yaml, .claude/knowledge/DEPENDENCY_GRAPH.yaml]
version_source: .claude/VERSION.yaml
observed_revision: 0169ecb  # rebound to HEAD 0169ecb by SYNC-072 (Consent repository synchronization pass, 2026-07-31) -- state-consent-record authoritative_writer summary row added (was entirely absent). Prior: rebound to HEAD 1aa5f88bce9ef27c9b0563363ebaa211d8aca240 by SYNC-055 (post-PR#243/#244/#245 repository-synchronization pass, 2026-07-28); content verified coherent 3254c83..1aa5f88 -- GD-18 calendar-absence narrow slice (PR #243), GD-16 decision + SPEC-DOCUMENTS-001 freeze (PR #244), backend-documents module implementation (PR #245) now reflected in-content (state-calendar-absence and state-worker-document nodes added, backend-documents module node added, stale backend-calendar orphan/stub notes corrected). Prior: rebound to HEAD 3254c83754f3660dee968c5138a32b6bc54d5578 by SYNC-054 (post-ADR-030 PR-1..PR-8 repository-synchronization pass, 2026-07-26); content verified coherent 9ca0eed..3254c83 -- ADR-030 capability-model rollout (REGIONAL_MANAGER role, capability-named gates replacing ManagerAdminGate, resolveHotelAccess/analytics regional_manager fixes) now reflected in permissions-middleware notes. Prior: rebound to HEAD 9ca0eed by SYNC-053 (post-#194 repository-synchronization + planning pass, 2026-07-23); content verified coherent 09e0b16..9ca0eed -- Epic 5/6/7/8 already reflected in-content, 367/367 backend tests green, tsc clean, repository-integrity-check.js exit 0. Prior: rebound by Repository Synchronization Workflow (Revision Rebind mode, SYNC-037) -- stamp-only, no content change; prior stamp 39fc1f5 (Package A, AUDIT-REPO-2026-07-14: ADR-016 propagated, state-audit-log authoritative_writer UNKNOWN -> backend-auth, stale collision_flags row removed) was two merges behind HEAD; content verified unchanged 39fc1f5..09e0b16

module_owners:  # every module owner is unassigned by evidence (SYNC-001)
  default: unassigned
  note: All 19 registered modules have owner=unassigned; no CODEOWNERS exists and backend/package.json author is empty.

contract_owners:
  - { contract: notification-service, owner_module: backend-notifications }
  - { contract: auth-middleware, owner_module: backend-auth }
  - { contract: prisma-schema, owner_module: unassigned }
  - { contract: base-service, owner_module: unassigned }
  - { contract: permissions-middleware, owner_module: unassigned }  # OQ-AUTH-07 reassignment pending (SYNC-011)
  - { contract: validation-middleware, owner_module: unassigned }

state_owners:  # authoritative writer per state domain (see STATE_OWNERSHIP_INDEX for full writer/reader sets)
  - { domain: state-session, authoritative_writer: backend-auth }
  - { domain: state-hotel, authoritative_writer: backend-crm }
  - { domain: state-hotel-worker, authoritative_writer: none }  # DORMANT since Epic 5 PR 5.8: backend-hotel-workers (the sole writer) was physically deleted; module retirement (ADR-022/ADR-024) executed via the separate EmploymentRecord build rather than in-place repurposing; see STATE_OWNERSHIP_INDEX.yaml for detail
  - { domain: state-job-request, authoritative_writer: backend-job-requests }  # renamed from state-work-request/backend-work-requests by Epic 9 PR 9.4 (TREQ-013/MIG-GAP-12). See SYNC-061, SYNC-062.
  # state-work-application entry REMOVED by Epic 9 PR 9.2 (TREQ-011/ADR-058): backend-work-applications
  # (sole writer) physically deleted; no row remains, see git history for the historical record.
  # See SYNC-059, SYNC-062.
  - { domain: state-worker-assignment, authoritative_writer: backend-assignments }
  - { domain: state-attendance, authoritative_writer: backend-attendance }
  - { domain: state-quality-verification, authoritative_writer: backend-quality }
  - { domain: state-rating, authoritative_writer: backend-quality }
  - { domain: state-worker-overall-rating, authoritative_writer: backend-quality }
  - { domain: state-notification, authoritative_writer: backend-notifications }
  - { domain: state-user, authoritative_writer: backend-auth }  # resolved by ADR-017 (Accepted, 2026-07-15); backend-users is a bounded profile writer/reader (non-security fields only); physical call sites unchanged by this record
  - { domain: state-audit-log, authoritative_writer: backend-auth }  # resolved by ADR-016 (Accepted, 2026-07-14); physical writes remain shared via BaseService.logAudit (unchanged by this record); backend-compliance is a read-only consumer, never a writer
  - { domain: state-hr-contract, authoritative_writer: backend-hr }  # added by HR implementation PR 1 (2026-07-30, migration 20260730010000); missing from this summary until the HR epic post-closeout synchronization pass -- see STATE_OWNERSHIP_INDEX.yaml for full writer/reader detail
  - { domain: state-hr-payslip-request, authoritative_writer: backend-hr }  # added by HR implementation PR 1 (2026-07-30, migration 20260730010000); missing from this summary until the HR epic post-closeout synchronization pass -- see STATE_OWNERSHIP_INDEX.yaml for full writer/reader detail
  - { domain: state-geo-checkin, authoritative_writer: backend-geo }  # added by Geo implementation (2026-07-28, migration 20260728010000_add_worker_geo_checkin, GD-14); missing from this summary until the Geo repository synchronization pass (2026-07-30) -- see STATE_OWNERSHIP_INDEX.yaml for full writer/reader detail
  - { domain: state-consent-record, authoritative_writer: backend-consent }  # added by Consent implementation (2026-07-31, PR #293, migration 20260731000000_add_consent_record, GD-17/ADR-037); missing from this summary until the Consent repository synchronization pass (2026-07-31) -- see STATE_OWNERSHIP_INDEX.yaml for full writer/reader detail

collision_flags:  # bearing-unknown ownership that G1.5 must escalate rather than silently pass
  - all module/contract owners: unassigned pending human assignment (SYNC-001)

invalidated_by: [architecture or ownership decision, MODULE_REGISTRY/DEPENDENCY_GRAPH revision change]

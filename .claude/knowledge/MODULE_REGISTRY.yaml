schema_version: 1
project: hotel-crm
observed_revision: 9ca0eed45dec34aa4ce3739da4bc8e4c117f6c3f  # rebound to HEAD 9ca0eed by SYNC-053 (post-#194 repository-synchronization + planning pass, 2026-07-23); content verified coherent 09e0b16..9ca0eed -- Epic 5/6/7/8 already reflected in-content, 367/367 backend tests green, tsc clean, repository-integrity-check.js exit 0. Prior: rebound by Repository Synchronization Workflow (Revision Rebind mode, SYNC-037) -- stamp-only, no content change; prior stamp 39fc1f5 (Package A, AUDIT-REPO-2026-07-14: backend-calendar.specification set to SPEC-CALENDAR-001@0.1.0 REVIEW, AUDIT-L1; backend-compliance/backend-retention zero-code-deferral rationale added to unresolved:, AUDIT-M1; edge-analytics-reads-hotel-worker unresolved: note updated to APPLIED, AUDIT-M3) was two merges behind HEAD; content verified unchanged 39fc1f5..09e0b16
status: verified-inventory

# Inventory is verified against the worktree at the observed revision.
# Ownership remains BLOCKED: no CODEOWNERS file exists and backend/package.json
# "author" is empty; owner assignment is reserved human authority (SYNC-001).
# Every `owner` is `unassigned` by evidence, never invented.
# No event bus exists in the codebase: all `published_events`/`consumed_events`
# are `none-observed`. The per-module `dependencies` field lists only direct
# synchronous module->module service-call edges (into backend-notifications).
# Broader relationships -- cross-module Prisma reads-state/writes-state, client
# consumes-api, and operations deploys-to edges -- are recorded authoritatively in
# DEPENDENCY_GRAPH.yaml, not duplicated here. All modules share Prisma/PostgreSQL
# via a shared BaseService (shared infrastructure, not a module contract).
# STATUS AS OF 2026-07-20 (Repository Synchronization Workflow, superseding the stale
# "no frozen module specification exists" claim this block previously made): ten module
# specifications are now FROZEN (G2 Specification Freeze, reserved human authority) --
# SPEC-AUTH-001@0.3.0, SPEC-JOB-DISPATCH-001@0.3.1 (backend-work-requests/work-applications/
# assignments), SPEC-ATT-001@0.2.0, SPEC-USERS-001@0.2.0, SPEC-CALENDAR-001@0.3.0,
# SPEC-EMP-001@0.2.0, SPEC-CRM-001@0.2.0, SPEC-NOTIF-001@0.2.0, SPEC-QUAL-001@0.2.0, and
# SPEC-ANALYTICS-001@0.2.0 -- see each module's own `specification` field below for its
# freeze date and disposition, and MODULE_MEMORY.yaml for the corroborating ART-MEM summary
# now produced for all ten (backfilled for backend-auth/work-requests/attendance by this
# pass; the remaining seven were produced at their own freeze). Every FROZEN module carries
# open security/decision/owner-assignment items forward as G8 release prerequisites, not
# freeze blockers, per the human ruling first applied to SPEC-AUTH-001/SPEC-JOB-DISPATCH-001/
# SPEC-ATT-001 on 2026-07-15. Only backend-chatbot (SPEC-CHATBOT-001@0.1.3) and backend-geo
# (SPEC-GEO-001@0.1.1) remain REVIEW-status, both unimplemented stubs blocked on open
# decisions (see SPECIFICATION_INDEX.yaml). backend-hotel-workers is RETIRED (ADR-022).

modules:
  - id: backend-auth
    type: backend-module
    path: backend/src/modules/auth
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-AUTH-001@0.3.0 (FROZEN)  # docs/03-modules/auth/MODULE_SPEC.md; G4 reviews complete (architecture/dependency/consistency/performance all PASS_WITH_ACTIONS, zero Critical/High); security review FAIL (1 Critical, 4 High) against live code -- unauthenticated token-less password-reset account takeover (Critical), JWT refresh-secret fallback, checkHotelAccess admin/manager/checker blanket bypass, cleartext Session.refresh_token storage, and absent MFA (all High); G6-doc PASS_WITH_ACTIONS. NOT frozen (G2 human); Security FAIL blocks G2 pending a code fix or an authorized Risk Assessment (see SYNC_STATE.yaml SYNC-011). UPDATE 2026-07-10 (SYNC-018/HOTFIX-AUTH-002): the Critical (password-reset account takeover) is RESOLVED in code -- see SIR-AUTH-001. The 4 High findings are unaffected and remain open; the spec's own Security G4 gate has not been re-run (that is a Documentation Workflow activity, not performed by this hotfix) so `specification` status here still reflects the FAIL recorded against SYNC-011 for the 4 remaining High items. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. state-user dual-writer ambiguity resolved by ADR-017 (Accepted, 2026-07-15).
    evidence: backend/src/routes/v1/index.ts:22

  - id: backend-users
    type: backend-module
    path: backend/src/modules/users
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-USERS-001@0.2.0 (FROZEN)  # FROZEN at G2 on 2026-07-19 by the commissioning human (SYNC-041), reusing the completed G4 evidence; zero open Critical/High. state-user ownership cleared by ADR-017 (Accepted 2026-07-15), SYNC-005 RESOLVED. Open decisions OQ-USERS-01/02/05/06 + SIR-USERS-005 (Medium) and owner assignment SYNC-001/OQ-USERS-07 remain implementation/release prerequisites reviewed by G8, NOT freeze blockers (SPEC-AUTH-001/SPEC-ATT-001 precedent). See SYNC_STATE.yaml SYNC-021/SYNC-041; MODULE_MEMORY.yaml ART-MEM-backend-users.
    evidence: backend/src/routes/v1/index.ts:23
    # UPDATE 2026-07-11 (SYNC-020/HOTFIX-AUTH-003): the createUser privilege-escalation
    # gap independently found by SPEC-USERS-001's own security review (SIR-USERS-003
    # facet a) is RESOLVED in code -- createUser now mirrors updateUser's admin-role-
    # assignment guard (service.ts:96-98, line shifted after the guard's own comment
    # block was added; content unchanged). Two non-blocking follow-ups remain open,
    # tracked in the Auth section of the Specification Issues Register (SIR-AUTH-019
    # target-admin-protection gap; SIR-AUTH-020 guard-duplication cleanup). This UPDATE
    # note records live code state only; it does not re-run SPEC-USERS-001's own G4
    # gate (a Documentation Workflow activity, not performed here), mirroring the
    # pattern already used for backend-auth/HOTFIX-AUTH-002 (SYNC-018).

  - id: backend-crm
    type: backend-module
    path: backend/src/modules/crm
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-CRM-001@0.2.0 (FROZEN)  # docs/03-modules/crm/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20 (G2 Approval Workflow). G4 reviews complete (architecture/dependency/consistency/security/performance all PASS_WITH_ACTIONS, zero Critical/High), reused unchanged for freeze. OD-CRM-01's Hotel Group facet RESOLVED by ADR-023 (HotelGroup entity owned by backend-crm); dedicated-Hotel-Manager facet plus OD-CRM-02/03/04/05(implementation)/06..17 and owner assignment (SYNC-001) are release/G8 prerequisites, not freeze blockers.
    evidence: backend/src/routes/v1/index.ts:24

  - id: backend-hotel-workers
    type: backend-module
    path: backend/src/modules/hotel-workers
    owner: unassigned
    lifecycle: removed  # Epic 5 PR 5.8 (ADR-022 roadmap step 6, ADR-024 D5): both removal conditions held (PR 5.5+5.7 complete — no authz reader, no roster reader of HotelWorker remained) so the module (routes/controller/service/types) and its route mount were physically deleted. CODE-ONLY retirement per explicit human decision (2026-07-22): the underlying `HotelWorker`/`HotelWorkerStatus` schema is deliberately left untouched in schema.prisma (no migration, no DROP TABLE) — ADR-022's Data Migration Plan states "No DROP TABLE" for its original in-place-repurposing design, and no backfill of HotelWorker rows into EmploymentRecord has ever been built or run. The table is dormant: unread by any application code except the new read-only `employment-record:backfill-status` reporting tool (backend/src/scripts/employment-record-backfill-status.ts), which exists solely to inform a future human decision on the table's fate.
    implementation_status: removed
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: RETIRED (ADR-022)  # module removed; state-hotel-worker itself remains schema-only, dormant (see state-hotel-worker note below).
    evidence: git history (removed by Epic 5 PR 5.8); backend/prisma/schema.prisma (HotelWorker model, dormant, unchanged)

  - id: backend-work-requests
    type: backend-module
    path: backend/src/modules/work-requests
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: [backend-notifications]
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-JOB-DISPATCH-001@0.3.1 (FROZEN)  # docs/03-modules/job-dispatch/MODULE_SPEC.md; one bounded capability with work-applications + assignments. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. Architecture BLOCKED cleared by ADR-018 (accept-transaction coupling superseded-by-pivot, Accepted 2026-07-15). Amended 0.3.0->0.3.1 (2026-07-19, Correction) per ADR-021 (Calendar boundary): CalendarEntry narrowed to assignment-kind only; TREQ-009/TRULE-008 auto-cancel converted to consuming EVT-CAL-SickVacationMarked; sick/vacation manager notification ceded to Calendar. FROZEN status retained (narrow reconciliation, not a re-open); see SYNC_STATE.yaml SYNC-042.
    evidence: backend/src/routes/v1/index.ts:26; backend/src/modules/work-requests/service.ts:4

  - id: backend-work-applications
    type: backend-module
    path: backend/src/modules/work-applications
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: [backend-notifications]
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-JOB-DISPATCH-001@0.3.1 (FROZEN)  # docs/03-modules/job-dispatch/MODULE_SPEC.md. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. Architecture BLOCKED cleared by ADR-018. Amended 0.3.0->0.3.1 (2026-07-19, Correction) per ADR-021; see backend-work-requests entry.
    evidence: backend/src/routes/v1/index.ts:27; backend/src/modules/work-applications/service.ts:12

  - id: backend-assignments
    type: backend-module
    path: backend/src/modules/assignments
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-JOB-DISPATCH-001@0.3.1 (FROZEN)  # docs/03-modules/job-dispatch/MODULE_SPEC.md. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. Architecture BLOCKED cleared by ADR-018. Amended 0.3.0->0.3.1 (2026-07-19, Correction) per ADR-021; see backend-work-requests entry.
    evidence: backend/src/routes/v1/index.ts:28

  - id: backend-attendance
    type: backend-module
    path: backend/src/modules/attendance
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: [backend-notifications]
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-ATT-001@0.2.0 (FROZEN)  # docs/03-modules/attendance/MODULE_SPEC.md; G4 reviews complete: architecture BLOCKED (2 High -- unratified cross-owner-write Decision Record OQ-03, unassigned ownership SYNC-001), security FAIL (1 High -- cross-tenant hotel-scoping OQ-02), dependency/consistency/performance all PASS_WITH_ACTIONS (zero Critical/High); all author-fixable Medium/Low findings applied at v0.1.1. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. Architecture BLOCKED cleared by ADR-018 (EXPECTED-seed cross-owner coupling superseded-by-pivot, Accepted 2026-07-15); the 1 High security finding (cross-tenant hotel-scoping OQ-02) is a release prerequisite (see SYNC_STATE.yaml SYNC-019).
    evidence: backend/src/routes/v1/index.ts:29; backend/src/modules/attendance/service.ts:4

  - id: backend-quality
    type: backend-module
    path: backend/src/modules/quality
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: [backend-notifications]
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-QUAL-001@0.2.0 (FROZEN)  # docs/03-modules/quality/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20 (G2 Approval Workflow). G4 reviews complete (architecture/dependency/consistency/security/performance all PASS_WITH_ACTIONS, zero Critical/High), reused unchanged for freeze; G6-doc PASS_WITH_ACTIONS. OQ-01..09 and owner assignment (OQ-06/SYNC-001) are release/G8 prerequisites; OQ-03/OQ-09 (Medium, cross-tenant) routed to Risk Assessment.
    evidence: backend/src/routes/v1/index.ts:30; backend/src/modules/quality/service.ts:4

  - id: backend-hr
    type: backend-module
    path: backend/src/modules/hr
    owner: unassigned
    lifecycle: active
    implementation_status: active-no-tests  # registered and implemented; no dedicated test file in backend/src/__tests__/
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-EMP-001@0.2.0 (FROZEN)  # docs/03-modules/employee-management/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20 (G2 Approval Workflow, conditional authorization contingent on Security pass). G4: architecture/dependency/consistency/performance PASS_WITH_ACTIONS; security PASS_WITH_ACTIONS (upgraded from FAIL by targeted re-verification -- OD-EMP-05/checkHotelAccess scoping resolved by ADR-023; special-category access/audit controls unbuilt but adequately disclosed). Both remaining High findings are release/G8 prerequisites, not freeze blockers, per SPEC-AUTH-001/SPEC-ATT-001 precedent.
    related_specification: SPEC-HR-001@0.2.1 (REVIEW, architecture BLOCKED)  # DISCOVERABILITY CROSS-REFERENCE, added by Repository Synchronization Workflow 2026-07-20 -- not a second specification field, not an alternate mapping. docs/03-modules/hr/MODULE_SPEC.md targets this SAME registry id/code path (backend/src/modules/hr) for a DIFFERENT capability set (contract lifecycle/generation, payslip-request-to-fulfilment, contract-scan upload) than the FROZEN SPEC-EMP-001 above (employment record/lifecycle). The two specifications' boundary claims conflict with each other and with docs/03-modules/onboarding/MODULE_SPEC.md over contract generation/confirmation ownership -- disclosed, not resolved, at OD-HR-01a/OD-HR-01b (docs/03-modules/hr/MODULE_SPEC.md) and OD-EMP-10 (this module's own frozen spec, which itself states the docs<->code mapping to backend-hr is "proposed, not asserted"). This note does not change architecture, does not merge responsibilities, and does not resolve OD-HR-01a/OD-EMP-10 -- those remain reserved human/architecture decisions. It exists solely so a reader of this registry entry discovers that a second, non-frozen specification also claims this module id before starting implementation against it.
    evidence: backend/src/routes/v1/index.ts:31

  - id: employee-management
    type: backend-module
    path: backend/src/modules/employee-management
    owner: unassigned  # SYNC-001 still open; owner assignment is reserved human authority, unchanged by this build.
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed  # OD-EMP-09: no event bus; EVT-EMP-* emitted as structured log lines only.
    consumed_events: none-observed
    specification: SPEC-EMP-001@0.2.0 (FROZEN)  # docs/03-modules/employee-management/MODULE_SPEC.md. Employment-record build (Epic 5 PR 5.6). OD-EMP-10 (docs<->code mapping, previously "proposed, not asserted" and reserved to human/SYNC-001) resolved by a human decision on 2026-07-22 to a DEDICATED module here (not backend-hr), keeping SPEC-EMP-001's employment-record capability physically separate from backend-hr's SPEC-HR-001 contracts/payroll surface. Owned state added additively (state-employment-record, state-employee-blocklist); HotelWorker retained untouched as the ADR-022 compatibility layer (consumer repointing and roster migration are PR 5.7/5.8, not this PR). Provisional-at-implementation items: OD-EMP-16 perf budget (conservative bounds applied, PERF-EMP-002), OD-EMP-08 importer roles (Admin-only), special-category audience (Admin-only pending the payslip-processor role). NOTE: backend-hr's own registry row above still records SPEC-EMP-001 as a historical mapping plus its SPEC-HR-001 collision disclosure; full reconciliation of that row and owner assignment remain a human/SYNC-001 follow-up.
    evidence: backend/src/routes/v1/index.ts (/employees, FEATURE_EMPLOYMENT_RECORD-gated); backend/src/modules/employee-management/

  - id: backend-notifications
    type: backend-module
    path: backend/src/modules/notifications
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-NOTIF-001@0.3.0 (FROZEN)  # docs/03-modules/notifications/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20; amended to 0.3.0 (Decision-Integration) 2026-07-23 by ADR-029 (GD-01) — resolves OQ-NOTIF-01(dispatch)/-04/-06/-07/-08/-09; Transactional Outbox + Worker runtime. G4 reviews (architecture/dependency/consistency/security/performance PASS_WITH_ACTIONS, zero Critical/High) reused unchanged. Remaining open decisions OQ-NOTIF-02/-03/-05 and owner assignment (SYNC-001) are release/G8 prerequisites, not freeze blockers.
    evidence: backend/src/routes/v1/index.ts:32

  - id: backend-analytics
    type: backend-module
    path: backend/src/modules/analytics
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-ANALYTICS-001@0.2.0 (FROZEN)  # docs/03-modules/analytics/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20 (G2 Approval Workflow). G4 reviews complete (architecture/dependency/consistency/performance all PASS_WITH_ACTIONS; security upgraded FAIL -> PASS_WITH_ACTIONS -- the Critical OQ-ANALYTICS-01 leaderboard-authz gap was RESOLVED in code 2026-07-17 by Sprint 0 S0-5, see SIR-ANLY-001, independently re-verified against live routes.ts at freeze time; Medium OQ-ANALYTICS-11 and new Low OQ-ANALYTICS-12 non-blocking); G6-doc PASS_WITH_ACTIONS.
    evidence: backend/src/routes/v1/index.ts:33

  - id: backend-calendar
    type: backend-module
    path: backend/src/modules/calendar
    owner: unassigned
    lifecycle: active
    implementation_status: stub  # route-registered but all CalendarService methods throw NotImplementedError (backend/src/modules/calendar/service.ts:5-11); reconciled from the prior mislabel active-no-tests per OD-CAL-05 / ADR-021 re-author (2026-07-19)
    dependencies: []
    published_events: none-observed  # current code; target (ADR-021): publishes EVT-CAL-SickVacationMarked
    consumed_events: none-observed
    specification: SPEC-CALENDAR-001@0.3.0 (FROZEN)  # docs/03-modules/calendar/MODULE_SPEC.md. FROZEN at G2 on 2026-07-19 by the commissioning human, on the ADR-021 boundary (narrow Calendar: owns state-calendar-absence + availability read-model; Job Dispatch retains WorkerAssignment/CalendarEntry/exclusivity). All five G4 dimensions PASS_WITH_ACTIONS or better, zero open Critical/High. Open decisions OD-CAL-04/06/07/08/10/11 + owner assignment OD-CAL-09/SYNC-001 are release/implementation prerequisites reviewed by G8, NOT freeze blockers. See SYNC_STATE.yaml SYNC-042/043/044; DECISION_INDEX ADR-021 (Accepted); MODULE_MEMORY.yaml ART-MEM-backend-calendar
    evidence: backend/src/routes/v1/index.ts:34

  - id: backend-chatbot
    type: backend-module
    path: backend/src/modules/chatbot
    owner: unassigned
    lifecycle: declared
    implementation_status: unimplemented-stub  # directory holds only .placeholder; not route-registered
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-CHATBOT-001@0.1.3 (REVIEW)  # G4 round complete (architecture/dependency/consistency/performance/security all PASS_WITH_ACTIONS, zero Critical/High); G2 freeze remains reserved human authority, blocked on OD-CHAT-005/006/013 (SYNC-029)
    evidence: backend/src/modules/chatbot/.placeholder

  - id: backend-geo
    type: backend-module
    path: backend/src/modules/geo
    owner: unassigned
    lifecycle: declared
    implementation_status: unimplemented-stub  # directory holds only .placeholder; not route-registered
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-GEO-001@0.1.1 (REVIEW)  # G4 round complete (architecture/dependency/performance PASS, consistency PASS, security PASS_WITH_ACTIONS); G2 freeze reserved human authority, blocked on OD-GEO-001/002 (ownership split) and owner assignment (SYNC-001)
    evidence: backend/src/modules/geo/.placeholder

  - id: frontend-web
    type: web-client
    path: frontend
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: frontend/package.json; ecosystem.config.js:25 (hotel-crm-web); nginx/hotelcrm.conf:92 (proxy_pass to :3000)

  - id: mobile-worker
    type: mobile-client
    path: mobile/worker-app
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: mobile/worker-app/package.json; package.json:9 (mobile/* workspace)

  - id: mobile-checker
    type: mobile-client
    path: mobile/checker-app
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: mobile/checker-app/package.json; package.json:9 (mobile/* workspace)

  - id: operations
    type: infrastructure
    path: "."
    owner: unassigned
    lifecycle: active
    implementation_status: not-applicable  # infrastructure surface, not a backend service module
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: docker-compose.yml; docker-compose.prod.yml; ecosystem.config.js; nginx/hotelcrm.conf; deploy/aws-edge-checklist.md; scripts/deploy.sh

ownership_conflicts: []

unresolved:
  - All accountable owners require authoritative human assignment (SYNC-001, blocked); no CODEOWNERS exists and backend/package.json "author" is empty.
  - backend-chatbot and backend-geo are declared-but-unimplemented stubs (directories contain only .placeholder; not route-registered).
  - backend-hr and backend-calendar are active but lack a dedicated test file in backend/src/__tests__/.
  - SUPERSEDED (2026-07-20, Repository Synchronization Workflow): the prior claim here ("no frozen module specification exists yet") is stale -- ten module specifications are now FROZEN (see the file header above and each module's own `specification` field); only backend-chatbot/backend-geo remain REVIEW-status, both unimplemented stubs. Retained as historical trail, not current state.
  - backend-analytics's `GET /analytics/leaderboard`(`/by-hotel/:hotel_id`) Critical authz gap (OQ-ANALYTICS-01 / SIR-ANLY-001) is now RESOLVED in code by Sprint 0 item S0-5 (EPIC-SECREM, 2026-07-17): both routes enforce `requireRole(['admin','manager'])`, and `/by-hotel/:hotel_id` additionally enforces `checkHotelAccess()`, matching the module's own `/stats`/`/hotel-summary` and the sibling `/quality/leaderboard`. Independent security review PASS (Critical closed); backend suite green (149/149). The Critical no longer blocks SPEC-ANALYTICS-001's G2; the residual manager cross-tenant visibility on `/by-hotel` and the un-gated `getHotelSummary.top_workers` slice are tracked non-blocking under SIR-ANLY-014 (downstream of SIR-AUTH-003).
  - `edge-analytics-reads-hotel-worker` (dependency review, SPEC-ANALYTICS-001 v0.1.1 FIND-DEP-001) is now APPLIED to DEPENDENCY_GRAPH.yaml by the repository-synchronization pass (Package A step 5, AUDIT-M3); `state-hotel-worker.readers` now includes `backend-analytics`.
  - `backend-consent` (target `SPEC-CONSENT-001@0.1.1`, status `REVIEW`) has zero code footprint — no `backend/src/modules/consent` directory exists at all (not even a `.placeholder`, unlike `backend-chatbot`/`backend-geo`) — so no module entry is added here, mirroring the `backend-documents`/`backend-payslips` zero-code deferral pattern. `docs/03-modules/consent/MODULE_SPEC.md` now exists (authored under `ADR-015`, 2026-07-13; completed its first G4 Independent Review round — Architecture/Dependency/Consistency/Security/Performance all `PASS_WITH_ACTIONS`, zero Critical/High findings remaining after v0.1.0→v0.1.1 correction — and G6 documentation validation, `PASS_WITH_ACTIONS`; see `.claude/governance/SPECIFICATION_ISSUES_REGISTER.md`'s `## Module: Consent` section, `SIR-CONSENT-001..011`). `ADR-015` (Accepted, 2026-07-13) pre-emptively settles the ownership boundary: `backend-consent` is the exclusive owner of consent lifecycle/records/versions/withdrawal/renewal/audit history/validation, covering the recurring GDPR access gate (CRR §24) five specs already anticipate plus the one-time chatbot-consent question in `docs/03-modules/onboarding/MODULE_SPEC.md` `OPQ-3` (resolved by that ADR). Not frozen (G2 human) — owner assignment and all 11 open decisions remain standing human-authority items. See `BOUNDARY_INDEX.yaml`'s `backend-consent` row and `DECISION_INDEX.md`.
  - `backend-compliance` (target `SPEC-COMPLIANCE-001@0.1.0`, status `REVIEW`) has zero code footprint — no `backend/src/modules/compliance` directory exists at all — so no module entry is added here, mirroring the `backend-consent`/`backend-documents`/`backend-payslips` zero-code deferral pattern. `docs/03-modules/compliance/MODULE_SPEC.md` now exists (authored 2026-07-14). `ADR-016` (Accepted, 2026-07-14) resolved the pre-authoring G1.5 Boundary Collision Gate FAIL against `state-audit-log`: `backend-auth` is the authoritative writer; `backend-compliance` is a read-only consumer, never a writer or lifecycle owner, of `AuditLog` (backend-auth), Consent (backend-consent, per ADR-015), Retention's deletion-audit log (backend-retention), and Documents' worker-scoped retrievability (backend-documents). Not frozen (G2 human) — owner assignment (SYNC-001) and OD-COMPLIANCE-002..012 remain standing human-authority items. See `BOUNDARY_INDEX.yaml`'s `backend-compliance` row.
  - `backend-retention` (target `SPEC-RETENTION-001@0.2.0`, status `REVIEW`) has zero code footprint — no `backend/src/modules/retention` directory exists at all — so no module entry is added here, mirroring the `backend-consent`/`backend-compliance` zero-code deferral pattern. `docs/03-modules/retention/MODULE_SPEC.md` now exists (authored 2026-07-14), owning the three-tier GDPR retention-classification model, `RetentionLog`, the daily automatic-deletion sweep job, and `IF-RETENTION-*` interfaces. G1.5 PASS at authoring (no other module claimed retention-execution ownership). Not frozen (G2 human) — owner assignment and OD-RETENTION-01/05/10/14 remain standing human-authority items. See `BOUNDARY_INDEX.yaml`'s `backend-retention` row.

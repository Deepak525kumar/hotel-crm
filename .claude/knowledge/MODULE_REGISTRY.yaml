schema_version: 1
project: hotel-crm
observed_revision: 1aa5f88bce9ef27c9b0563363ebaa211d8aca240  # rebound to HEAD 1aa5f88bce9ef27c9b0563363ebaa211d8aca240 by SYNC-055 (post-PR#243/#244/#245 repository-synchronization pass, 2026-07-28); content verified coherent 3254c83..1aa5f88 -- GD-18 calendar-absence narrow slice (PR #243), GD-16 decision + SPEC-DOCUMENTS-001 freeze (PR #244), backend-documents module implementation (PR #245) now reflected in-content (state-calendar-absence and state-worker-document nodes added, backend-documents module node added, stale backend-calendar orphan/stub notes corrected). Prior: rebound to HEAD 3254c83754f3660dee968c5138a32b6bc54d5578 by SYNC-054 (post-ADR-030 PR-1..PR-8 repository-synchronization pass, 2026-07-26); content verified coherent 9ca0eed..3254c83 -- ADR-030 capability-model rollout (REGIONAL_MANAGER role, capability-named gates replacing ManagerAdminGate, resolveHotelAccess/analytics regional_manager fixes) now reflected in permissions-middleware notes. Prior: rebound to HEAD 9ca0eed by SYNC-053 (post-#194 repository-synchronization + planning pass, 2026-07-23); content verified coherent 09e0b16..9ca0eed -- Epic 5/6/7/8 already reflected in-content, 367/367 backend tests green, tsc clean, repository-integrity-check.js exit 0. Prior: rebound by Repository Synchronization Workflow (Revision Rebind mode, SYNC-037) -- stamp-only, no content change; prior stamp 39fc1f5 (Package A, AUDIT-REPO-2026-07-14: backend-calendar.specification set to SPEC-CALENDAR-001@0.1.0 REVIEW, AUDIT-L1; backend-compliance/backend-retention zero-code-deferral rationale added to unresolved:, AUDIT-M1; edge-analytics-reads-hotel-worker unresolved: note updated to APPLIED, AUDIT-M3) was two merges behind HEAD; content verified unchanged 39fc1f5..09e0b16
status: verified-inventory

# Inventory is verified against the worktree at the observed revision.
# Ownership remains BLOCKED: no CODEOWNERS file exists and backend/package.json
# "author" is empty; owner assignment is reserved human authority (SYNC-001).
# Every `owner` is `unassigned` by evidence, never invented.
# No event bus exists in the codebase: all `published_events`/`consumed_events`
# are `none-observed`. The per-module `dependencies` field lists only direct
# synchronous module->module service-call edges (into backend-notifications).
# Broader relationships -- cross-module Prisma reads-state/writes-state, client
# consumes-api, and operations deploys-to edges -- are recorded authoritatively in
# DEPENDENCY_GRAPH.yaml, not duplicated here. All modules share Prisma/PostgreSQL
# via a shared BaseService (shared infrastructure, not a module contract).
# STATUS AS OF 2026-07-20 (Repository Synchronization Workflow, superseding the stale
# "no frozen module specification exists" claim this block previously made): ten module
# specifications are now FROZEN (G2 Specification Freeze, reserved human authority) --
# SPEC-AUTH-001@0.3.0, SPEC-JOB-DISPATCH-001@0.3.1 (backend-work-requests/work-applications/
# assignments), SPEC-ATT-001@0.2.0, SPEC-USERS-001@0.2.0, SPEC-CALENDAR-001@0.3.0,
# SPEC-EMP-001@0.2.0, SPEC-CRM-001@0.2.0, SPEC-NOTIF-001@0.2.0, SPEC-QUAL-001@0.2.0, and
# SPEC-ANALYTICS-001@0.2.0 -- see each module's own `specification` field below for its
# freeze date and disposition, and MODULE_MEMORY.yaml for the corroborating ART-MEM summary
# now produced for all ten (backfilled for backend-auth/work-requests/attendance by this
# pass; the remaining seven were produced at their own freeze). Every FROZEN module carries
# open security/decision/owner-assignment items forward as G8 release prerequisites, not
# freeze blockers, per the human ruling first applied to SPEC-AUTH-001/SPEC-JOB-DISPATCH-001/
# SPEC-ATT-001 on 2026-07-15. Only backend-chatbot (SPEC-CHATBOT-001@0.1.3) and backend-geo
# (SPEC-GEO-001@0.1.1) remain REVIEW-status, both unimplemented stubs blocked on open
# decisions (see SPECIFICATION_INDEX.yaml). backend-hotel-workers is RETIRED (ADR-022).
# backend-work-applications is RETIRED (TREQ-011 / ADR-058; Epic 9 PR 9.2, commits
# 12b1c31/2e89c4f, merged b035700) -- see SYNC-059.

modules:
  - id: backend-auth
    type: backend-module
    path: backend/src/modules/auth
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-AUTH-001@0.3.0 (FROZEN)  # docs/03-modules/auth/MODULE_SPEC.md; G4 reviews complete (architecture/dependency/consistency/performance all PASS_WITH_ACTIONS, zero Critical/High); security review FAIL (1 Critical, 4 High) against live code -- unauthenticated token-less password-reset account takeover (Critical), JWT refresh-secret fallback, checkHotelAccess admin/manager/checker blanket bypass, cleartext Session.refresh_token storage, and absent MFA (all High); G6-doc PASS_WITH_ACTIONS. NOT frozen (G2 human); Security FAIL blocks G2 pending a code fix or an authorized Risk Assessment (see SYNC_STATE.yaml SYNC-011). UPDATE 2026-07-10 (SYNC-018/HOTFIX-AUTH-002): the Critical (password-reset account takeover) is RESOLVED in code -- see SIR-AUTH-001. The 4 High findings are unaffected and remain open; the spec's own Security G4 gate has not been re-run (that is a Documentation Workflow activity, not performed by this hotfix) so `specification` status here still reflects the FAIL recorded against SYNC-011 for the 4 remaining High items. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. state-user dual-writer ambiguity resolved by ADR-017 (Accepted, 2026-07-15).
    evidence: backend/src/routes/v1/index.ts:22

  - id: backend-users
    type: backend-module
    path: backend/src/modules/users
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-USERS-001@0.2.0 (FROZEN)  # FROZEN at G2 on 2026-07-19 by the commissioning human (SYNC-041), reusing the completed G4 evidence; zero open Critical/High. state-user ownership cleared by ADR-017 (Accepted 2026-07-15), SYNC-005 RESOLVED. Open decisions OQ-USERS-01/02/05/06 + SIR-USERS-005 (Medium) and owner assignment SYNC-001/OQ-USERS-07 remain implementation/release prerequisites reviewed by G8, NOT freeze blockers (SPEC-AUTH-001/SPEC-ATT-001 precedent). See SYNC_STATE.yaml SYNC-021/SYNC-041; MODULE_MEMORY.yaml ART-MEM-backend-users.
    evidence: backend/src/routes/v1/index.ts:23
    # UPDATE 2026-07-11 (SYNC-020/HOTFIX-AUTH-003): the createUser privilege-escalation
    # gap independently found by SPEC-USERS-001's own security review (SIR-USERS-003
    # facet a) is RESOLVED in code -- createUser now mirrors updateUser's admin-role-
    # assignment guard (service.ts:96-98, line shifted after the guard's own comment
    # block was added; content unchanged). Two non-blocking follow-ups remain open,
    # tracked in the Auth section of the Specification Issues Register (SIR-AUTH-019
    # target-admin-protection gap; SIR-AUTH-020 guard-duplication cleanup). This UPDATE
    # note records live code state only; it does not re-run SPEC-USERS-001's own G4
    # gate (a Documentation Workflow activity, not performed here), mirroring the
    # pattern already used for backend-auth/HOTFIX-AUTH-002 (SYNC-018).

  - id: backend-crm
    type: backend-module
    path: backend/src/modules/crm
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-CRM-001@0.2.1 (FROZEN)  # docs/03-modules/crm/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20 (G2 Approval Workflow). G4 reviews complete (architecture/dependency/consistency/security/performance all PASS_WITH_ACTIONS, zero Critical/High), reused unchanged for freeze. OD-CRM-01's Hotel Group facet RESOLVED by ADR-023 (HotelGroup entity owned by backend-crm); dedicated-Hotel-Manager facet plus OD-CRM-02/03/05(implementation)/06..17 and owner assignment (SYNC-001) are release/G8 prerequisites, not freeze blockers. Amended 0.2.0->0.2.1 (2026-07-27, Correction) per GD-05: OD-CRM-04 (pause-jobs toggle) and OD-CRM-16 (bilateral cross-module contract) RESOLVED — Hotel.accepting_jobs implemented, enforced in work-requests/service.ts; reciprocal reference added to SPEC-JOB-DISPATCH-001@0.3.2.
    evidence: backend/src/routes/v1/index.ts:24

  - id: backend-hotel-workers
    type: backend-module
    path: backend/src/modules/hotel-workers
    owner: unassigned
    lifecycle: removed  # Epic 5 PR 5.8 (ADR-022 roadmap step 6, ADR-024 D5): both removal conditions held (PR 5.5+5.7 complete — no authz reader, no roster reader of HotelWorker remained) so the module (routes/controller/service/types) and its route mount were physically deleted. CODE-ONLY retirement per explicit human decision (2026-07-22): the underlying `HotelWorker`/`HotelWorkerStatus` schema is deliberately left untouched in schema.prisma (no migration, no DROP TABLE) — ADR-022's Data Migration Plan states "No DROP TABLE" for its original in-place-repurposing design, and no backfill of HotelWorker rows into EmploymentRecord has ever been built or run. The table is dormant: unread by any application code except the new read-only `employment-record:backfill-status` reporting tool (backend/src/scripts/employment-record-backfill-status.ts), which exists solely to inform a future human decision on the table's fate.
    implementation_status: removed
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: RETIRED (ADR-022)  # module removed; state-hotel-worker itself remains schema-only, dormant (see state-hotel-worker note below).
    evidence: git history (removed by Epic 5 PR 5.8); backend/prisma/schema.prisma (HotelWorker model, dormant, unchanged)

  - id: backend-job-requests  # renamed from backend-work-requests by Epic 9 PR 9.4 (WorkRequest -> JobRequest re-label off "marketplace" framing, TREQ-013/MIG-GAP-12); same live module, not a retirement. See SYNC-061.
    type: backend-module
    path: backend/src/modules/job-requests
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: [backend-notifications]
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-JOB-DISPATCH-001@0.3.8 (FROZEN)  # docs/03-modules/job-dispatch/MODULE_SPEC.md; one bounded capability with work-applications + assignments. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. Architecture BLOCKED cleared by ADR-018 (accept-transaction coupling superseded-by-pivot, Accepted 2026-07-15). Amended 0.3.0->0.3.1 (2026-07-19, Correction) per ADR-021 (Calendar boundary): CalendarEntry narrowed to assignment-kind only; TREQ-009/TRULE-008 auto-cancel converted to consuming EVT-CAL-SickVacationMarked; sick/vacation manager notification ceded to Calendar. FROZEN status retained (narrow reconciliation, not a re-open); see SYNC_STATE.yaml SYNC-042. Amended 0.3.1->0.3.2 (2026-07-27, Correction) per GD-05: REQ-044/RULE-014 added, acknowledging read dependency on Hotel.accepting_jobs (SPEC-CRM-001@0.2.1); closes SIR-CRM-016/OD-CRM-16's bilateral-contract requirement. Amended 0.3.2->0.3.3 (2026-07-29, Epic 9 PR 9.4) per TREQ-013/MIG-GAP-12: WorkRequest model renamed JobRequest (@@map("WorkRequest") preserves the physical table, zero data migration); module directory/class renamed backend-work-requests -> backend-job-requests; route mount path `/work-requests` retained unchanged (public API contract, not renamed). See SYNC-061. MODULE_SPEC.md itself had never received Review-and-Change-Log rows for either 0.3.2 or 0.3.3 (only narrated in Document Control prose) until Epic 9 PR 9.6's sync pass retroactively added both rows -- this registry entry's version citation was already correct, only the underlying spec document lagged. See SYNC-063. Amended 0.3.3->0.3.4 (2026-07-29, Epic 9 PR 9.7) per TREQ-002/TREQ-003/TREQ-010, MIG-GAP-04/MIG-GAP-05: new additive JobRequestSkillSlot model (migration 20260729040000_add_job_request_skill_slot) + JobRequestService.raiseBroadcast()/getBroadcastEligibility(); new routes POST /work-requests/broadcasts + GET /work-requests/broadcasts/:id/eligibility, gated by FEATURE_JOBDISPATCH_PHASE2 (default off). Closes the persistence half of TREQ-002, the eligibility-computation half of TREQ-003 (reuses backend-assignments' isWorkerFreeOnDay()/ACTIVE_ASSIGNMENT_STATUSES from PR 9.6), and the broadcast-path half of TREQ-010; does NOT close TREQ-003's notification-delivery half, TREQ-004, TREQ-005, or TREQ-006 (PR 9.8/9.9/9.10). Legacy JobRequest.position/workers_needed/workers_confirmed left untouched (still live for the marketplace flow). See SYNC-064. Amended 0.3.4->0.3.5 (2026-07-29, Epic 9 PR 9.8) per TREQ-003 delivery half, MIG-GAP-04: new additive NotificationType.JOB_REQUEST_BROADCAST value (migration 20260729050000_add_job_request_broadcast_notification_type) + JobRequestService.enqueueBroadcastNotifications(), called inside raiseBroadcast()'s existing transaction; reuses PR 9.7's eligibility computation (factored into a shared private computeBroadcastEligibility()) rather than recalculating it. OutboxSourceModule.WORK_REQUESTS reused unchanged (confirmed against PR 9.4's actual diff -- that rename never touched this enum). Closes TREQ-003's remaining notification-delivery half. See SYNC-066. Amended 0.3.5->0.3.6 (2026-07-30, Epic 9 PR 9.9) per TREQ-004/TREQ-005, MIG-GAP-06: new JobRequestService.acceptBroadcast() -- first-accept optimistic-concurrency arbitration via a transactional conditional updateMany on JobRequestSkillSlot.confirmed_count (no version column, per ADR-057's 2026-07-30 addendum confirming a bare count-guard is a conforming instance of the ratified mechanism at this per-skill-slot grain); on zero-affected-rows returns requirement_fulfilled (TREQ-005) instead of throwing; on success creates a WorkerAssignment with job_request_id set, work_request_id null, assigned_by_id = the broadcast's created_by_id (not the accepting worker, confirmed by the commissioning human). New route POST /work-requests/broadcasts/:id/accept, no requireRole gate (worker-initiated). Closes TREQ-004/TREQ-005; MIG-GAP-04 and MIG-GAP-06 both fully closed. See SYNC-067. Amended 0.3.6->0.3.7 (2026-07-30, Epic 9 PR 9.10) per TREQ-006/TRULE-005, MIG-GAP-09: new JobRequestAutoCloseJob (auto-close-job.ts, mirrors SessionSweepJob/GeoRetentionSweepJob shape), registered on the Platform Worker Scheduler (worker.ts); delegates to new JobRequestService.closeExpiredBroadcasts() (batched, closes any broadcast still OPEN 6h past creation, transitions to EXPIRED, notifies the raising manager via new additive NotificationType.JOB_REQUEST_CLOSED, migration 20260730000000_add_job_request_closed_notification_type). New JobRequestService.manualClose() + route POST /work-requests/broadcasts/:id/close (manager-initiated, requireRole) for an early close, sharing the same notification helper. OPEN->EXPIRED deliberately NOT added to the shared ALLOWED_TRANSITIONS table (marketplace flow has no EXPIRED-via-manual-action concept); manualClose() validates the precondition itself. Closes TREQ-006/TRULE-005; MIG-GAP-09 fully closed -- Epic 9's final planned PR (every TREQ/MIG-GAP row this module's target-state scope named is now built in code; SIR-JOBD-006 itself deliberately NOT closed by this pass, reserved for its own later act). See SYNC-068. Amended 0.3.7->0.3.8 (2026-07-30, documentation-correction pass following the Epic 9 Completion Audit) -- corrects five stale TREQ-001/007/011/012/013 status cells this module's own Review-and-Change-Log already narrated as built but whose Evidence-and-Traceability table still read "Target; unbuilt", and corrects MIG-GAP-07/010/011 attribution (Epic 8/SIR-JOBD-002; the Calendar module/ADR-032; Epic 1 PR 1.1/SIR-JOBD-001 respectively -- none built by Epic 9). No requirement, rule, or open-decision content changed. governance/SPECIFICATION_ISSUES_REGISTER.md's SIR-JOBD-006 closed RESOLVED in the same pass. See SYNC-069.
    evidence: backend/src/routes/v1/index.ts:29; backend/src/modules/job-requests/service.ts:1

  - id: backend-work-applications
    type: backend-module
    path: backend/src/modules/work-applications
    owner: unassigned
    lifecycle: removed  # Epic 9 PR 9.2 (commits 12b1c31/2e89c4f, merged b035700): TREQ-011 (ADR-058-eligible target, GD-20) required removing the WorkApplication model and all worker-initiated application endpoints (confirmed NON-GOAL of the marketplace-to-broadcast pivot). The module (routes/controller/service/types, backend/src/modules/work-applications/) was physically deleted, its route mount removed, and the WorkApplication model + ApplicationStatus enum + WorkerAssignment.application_id FK dropped from schema.prisma via migration 20260729000000_drop_work_application. Unlike the backend-hotel-workers precedent (ADR-022, CODE-ONLY: table left dormant, no migration), this retirement is CODE-AND-SCHEMA: the underlying table and enum are actually dropped, not merely orphaned.
    implementation_status: removed
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: RETIRED (TREQ-011 / ADR-058)  # module and its owning capability removed; docs/03-modules/job-dispatch/MODULE_SPEC.md retains the historical rule/endpoint rows marked `[RETIRED in target]`/`[TARGET: REMOVED]` for record purposes.
    evidence: git history (removed by Epic 9 PR 9.2, commits 12b1c31/2e89c4f, merged b035700); backend/prisma/migrations/20260729000000_drop_work_application/ (schema.prisma DROP)

  - id: backend-assignments
    type: backend-module
    path: backend/src/modules/assignments
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-JOB-DISPATCH-001@0.3.8 (FROZEN)  # docs/03-modules/job-dispatch/MODULE_SPEC.md. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. Architecture BLOCKED cleared by ADR-018. Amended 0.3.0->0.3.1 (2026-07-19, Correction) per ADR-021; 0.3.1->0.3.2 (2026-07-27, Correction) per GD-05; 0.3.2->0.3.3 (2026-07-29, Correction) per Epic 9 PR 9.4 (TREQ-013/MIG-GAP-12); 0.3.3->0.3.4 (2026-07-29, Correction) per Epic 9 PR 9.7 (TREQ-002/003/010, MIG-GAP-04/05) -- this module's own isWorkerFreeOnDay()/ACTIVE_ASSIGNMENT_STATUSES (PR 9.6) are now also consumed by backend-job-requests' getBroadcastEligibility(), a new intra-capability read dependency (see DEPENDENCY_GRAPH.yaml); 0.3.4->0.3.5 (2026-07-29, Correction) per Epic 9 PR 9.8 (TREQ-003 delivery half, MIG-GAP-04) -- unchanged for this module itself (PR 9.8 touches only backend-job-requests and backend-notifications' NotificationType enum); version citation bumped for consistency with the shared SPEC-JOB-DISPATCH-001 document. See backend-job-requests entry. 0.3.5->0.3.6 (2026-07-30, Correction) per Epic 9 PR 9.9 (TREQ-004/005, MIG-GAP-06) -- this module's WorkerAssignment model gains no new column (assigned_by_id's existing NOT NULL constraint is satisfied by the broadcast's created_by_id, not a schema change); version citation bumped for consistency. See backend-job-requests entry, SYNC-067. 0.3.6->0.3.7 (2026-07-30, Correction) per Epic 9 PR 9.10 (TREQ-006/005, MIG-GAP-09) -- this module's WorkerAssignment model is unaffected (auto-close/manual-close operate entirely on JobRequest, owned by backend-job-requests); version citation bumped for consistency. See backend-job-requests entry, SYNC-068. 0.3.7->0.3.8 (2026-07-30, documentation-correction pass following the Epic 9 Completion Audit) -- this module's WorkerAssignment/CalendarEntry models are unaffected (status/attribution corrections only, MIG-GAP-11's PATCH /assignments/:id authorization gap cross-referenced to the already-RESOLVED SIR-JOBD-001); version citation bumped for consistency with the shared SPEC-JOB-DISPATCH-001 document. See backend-job-requests entry, SYNC-069.
    evidence: backend/src/routes/v1/index.ts:30

  - id: backend-attendance
    type: backend-module
    path: backend/src/modules/attendance
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: [backend-notifications]
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-ATT-001@0.2.0 (FROZEN)  # docs/03-modules/attendance/MODULE_SPEC.md; G4 reviews complete: architecture BLOCKED (2 High -- unratified cross-owner-write Decision Record OQ-03, unassigned ownership SYNC-001), security FAIL (1 High -- cross-tenant hotel-scoping OQ-02), dependency/consistency/performance all PASS_WITH_ACTIONS (zero Critical/High); all author-fixable Medium/Low findings applied at v0.1.1. FROZEN at G2 Specification Freeze 2026-07-15 (G2 Approval Workflow); the spec is frozen independently of open security findings, which are RELEASE PREREQUISITES (review by G8), not freeze blockers, per the approving human ruling. Architecture BLOCKED cleared by ADR-018 (EXPECTED-seed cross-owner coupling superseded-by-pivot, Accepted 2026-07-15); the 1 High security finding (cross-tenant hotel-scoping OQ-02) is a release prerequisite (see SYNC_STATE.yaml SYNC-019).
    evidence: backend/src/routes/v1/index.ts:29; backend/src/modules/attendance/service.ts:4

  - id: backend-quality
    type: backend-module
    path: backend/src/modules/quality
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: [backend-notifications]
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-QUAL-001@0.2.0 (FROZEN)  # docs/03-modules/quality/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20 (G2 Approval Workflow). G4 reviews complete (architecture/dependency/consistency/security/performance all PASS_WITH_ACTIONS, zero Critical/High), reused unchanged for freeze; G6-doc PASS_WITH_ACTIONS. OQ-01..09 and owner assignment (OQ-06/SYNC-001) are release/G8 prerequisites; OQ-03/OQ-09 (Medium, cross-tenant) routed to Risk Assessment.
    evidence: backend/src/routes/v1/index.ts:30; backend/src/modules/quality/service.ts:4

  - id: backend-hr
    type: backend-module
    path: backend/src/modules/hr
    owner: unassigned
    lifecycle: active
    implementation_status: active-partial  # HR implementation PR 1/2/3 (2026-07-30, following the backend-hr/SPEC-EMP-001 boundary review): Contract/PayslipRequest schema landed; createContract/listContracts/getContractStatus/uploadSignedContract/confirmContractSigned real. createPayroll/listPayroll remain NotImplementedError (PR 4). uploadSignedContract/confirmContractSigned (PR 3) resolve the worker's current PENDING contract by worker_id (SPEC-HR-001's own Interfaces table carries no contract_id param); confirmContractSigned writes the REQ-HR-013/RULE-HR-15-mandated immutable audit record via BaseService.logAudit and starts the 1-year expiry clock (RULE-HR-05). RULE-HR-13/ADR-044's malware-scan hook (malware-scan.ts) is real reject-on-detection control flow with a disclosed pass-through default scanner -- ADR-044 explicitly leaves vendor/library selection undecided; no scanning library is a backend dependency. No dedicated test file existed before PR 2 -- hr-contract-lifecycle.test.ts, hr-malware-scan.test.ts added.
    dependencies: [employee-management, backend-documents]  # employee-management: reads EmploymentRecord.personal_data (edge-hr-reads-employment-record, PR 2, OD-HR-02b resolved). backend-documents: contract-scan upload delegation (MIG-GAP-DOC-001, pre-existing).
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-HR-001@0.2.9 (REVIEW)  # docs/03-modules/hr/MODULE_SPEC.md. CORRECTED by the backend-hr/SPEC-EMP-001 boundary review (2026-07-30): this field previously read SPEC-EMP-001 (employee-management's own spec, a different module at a different code path, backend/src/modules/employee-management) -- a leftover artifact from before employee-management was split into its own dedicated module (human decision, 2026-07-22, see that module's own registry entry below). ADR-012 (Accepted 2026-07-12) and ADR-014 (Accepted 2026-07-13) settle backend-hr as the exclusive owner of the contract-lifecycle and payslip-request capability; SPEC-HR-001 itself was corrected 2026-07-28 to mark OD-HR-01a/OD-HR-01b RESOLVED citing those ADRs. Not FROZEN -- G2 freeze remains blocked on OD-HR-15 (auth-dependency Risk Assessment) and ordinary G2 gate progression; OD-HR-02b (Personalfragebogen data-source ambiguity) was resolved by the boundary review (persisted employee-management record, not a transient Onboarding source -- no onboarding module exists in code) and is no longer a freeze blocker either.
    evidence: backend/src/routes/v1/index.ts:33; backend/src/modules/hr/service.ts; backend/prisma/migrations/20260730010000_add_hr_contract_payslip_request/

  - id: employee-management
    type: backend-module
    path: backend/src/modules/employee-management
    owner: unassigned  # SYNC-001 still open; owner assignment is reserved human authority, unchanged by this build.
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed  # OD-EMP-09: no event bus; EVT-EMP-* emitted as structured log lines only.
    consumed_events: none-observed
    specification: SPEC-EMP-001@0.2.0 (FROZEN)  # docs/03-modules/employee-management/MODULE_SPEC.md. Employment-record build (Epic 5 PR 5.6). OD-EMP-10 (docs<->code mapping, previously "proposed, not asserted" and reserved to human/SYNC-001) resolved by a human decision on 2026-07-22 to a DEDICATED module here (not backend-hr), keeping SPEC-EMP-001's employment-record capability physically separate from backend-hr's SPEC-HR-001 contracts/payroll surface. Owned state added additively (state-employment-record, state-employee-blocklist); HotelWorker retained untouched as the ADR-022 compatibility layer (consumer repointing and roster migration are PR 5.7/5.8, not this PR). Provisional-at-implementation items: OD-EMP-16 perf budget (conservative bounds applied, PERF-EMP-002), OD-EMP-08 importer roles (Admin-only), special-category audience (Admin-only pending the payslip-processor role). NOTE: backend-hr's own registry row above still records SPEC-EMP-001 as a historical mapping plus its SPEC-HR-001 collision disclosure; full reconciliation of that row and owner assignment remain a human/SYNC-001 follow-up.
    evidence: backend/src/routes/v1/index.ts (/employees, FEATURE_EMPLOYMENT_RECORD-gated); backend/src/modules/employee-management/

  - id: backend-notifications
    type: backend-module
    path: backend/src/modules/notifications
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-NOTIF-001@0.3.0 (FROZEN)  # docs/03-modules/notifications/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20; amended to 0.3.0 (Decision-Integration) 2026-07-23 by ADR-029 (GD-01) — resolves OQ-NOTIF-01(dispatch)/-04/-06/-07/-08/-09; Transactional Outbox + Worker runtime. G4 reviews (architecture/dependency/consistency/security/performance PASS_WITH_ACTIONS, zero Critical/High) reused unchanged. Remaining open decisions OQ-NOTIF-02/-03/-05 and owner assignment (SYNC-001) are release/G8 prerequisites, not freeze blockers.
    evidence: backend/src/routes/v1/index.ts:32

  - id: backend-analytics
    type: backend-module
    path: backend/src/modules/analytics
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-ANALYTICS-001@0.2.1 (FROZEN)  # docs/03-modules/analytics/MODULE_SPEC.md; FROZEN at G2 Specification Freeze 2026-07-20 (G2 Approval Workflow). G4 reviews complete (architecture/dependency/consistency/performance all PASS_WITH_ACTIONS; security upgraded FAIL -> PASS_WITH_ACTIONS -- the Critical OQ-ANALYTICS-01 leaderboard-authz gap was RESOLVED in code 2026-07-17 by Sprint 0 S0-5, see SIR-ANLY-001, independently re-verified against live routes.ts at freeze time; Medium OQ-ANALYTICS-11 and new Low OQ-ANALYTICS-12 non-blocking); G6-doc PASS_WITH_ACTIONS. Amended 0.2.0->0.2.1 (2026-07-27, Correction) per GD-06: OQ-ANALYTICS-02 resolved (worker-dashboard 403 + DashboardStats type-shape mismatch) -- new self-scoped GET /analytics/my-stats route, getWorkerStats() method, WorkerStats type; REQ-ANALYTICS-017/RULE-ANALYTICS-007 added.
    evidence: backend/src/routes/v1/index.ts:33

  - id: backend-calendar
    type: backend-module
    path: backend/src/modules/calendar
    owner: unassigned
    lifecycle: active
    implementation_status: active-partial  # 2026-07-27 (GD-18 narrow slice): GET/POST /calendar/my-absences (worker self-mark sick/vacation, REQ-CAL-T02/T03/T04/T08) are real, backed by the new CalendarAbsence model. The /operations endpoints (reception-data stub, OD-CAL-10, unrelated capability) still throw NotImplementedError, as does the manager weekly-plan placement view and the today-only availability read-model (REQ-CAL-T01/T06) -- neither built by this slice.
    dependencies: []
    published_events: none-observed  # same-day auto-cancel implemented as a direct in-process call to AssignmentService.update(), not the target EVT-CAL-SickVacationMarked event (no event bus exists yet, GD-12 undecided) -- a human-approved narrower mechanism, independently architecture-reviewed as still respecting the ADR-021 ownership boundary (ADR-021: Calendar never writes WorkerAssignment/CalendarEntry directly)
    consumed_events: none-observed
    specification: SPEC-CALENDAR-001@0.3.0 (FROZEN)  # docs/03-modules/calendar/MODULE_SPEC.md. FROZEN at G2 on 2026-07-19 by the commissioning human, on the ADR-021 boundary (narrow Calendar: owns state-calendar-absence + availability read-model; Job Dispatch retains WorkerAssignment/CalendarEntry/exclusivity). All five G4 dimensions PASS_WITH_ACTIONS or better, zero open Critical/High. Open decisions OD-CAL-04/06/07/08/10/11 + owner assignment OD-CAL-09/SYNC-001 are release/implementation prerequisites reviewed by G8, NOT freeze blockers. See SYNC_STATE.yaml SYNC-042/043/044; DECISION_INDEX ADR-021 (Accepted); MODULE_MEMORY.yaml ART-MEM-backend-calendar. Implementation (2026-07-27, GD-18 narrow slice) landed state-calendar-absence (CalendarAbsence model, migrations 20260727040000/20260727050000) and GET/POST /calendar/my-absences; OD-CAL-04 (timezone) implementation-time default: Europe/Berlin (matches Hotel.timezone default, SPEC-CRM-001); OD-CAL-06 (notification transport) implementation-time default: existing notificationService.enqueue() in-process call, not a formal event.
    evidence: backend/src/modules/calendar/service.ts; backend/src/routes/v1/index.ts:34

  - id: backend-documents
    type: backend-module
    path: backend/src/modules/documents
    owner: unassigned
    lifecycle: active-partial
    implementation_status: active-partial  # 2026-07-27 (PR #245, GD-16): all five IF-DOC-* target interfaces built (upload/list/get/completeness/export) and mounted at /v1/documents. GD-16's actor model implemented exactly: worker self-scope enforced in DocumentService (RULE-DOC-08, actor_id === worker_id), manager-upload group-scoped via existing checkWorkerScope() middleware (admin bypasses). Known gaps, explicitly deferred not silent: no multipart/file-parsing middleware wired yet (controller passes Buffer.alloc(0), the same gap the migrated-from backend-hr stub had); storage.ts's real S3 client code path exists but @aws-sdk/client-s3/@aws-sdk/s3-request-presigner are not yet package.json dependencies (stub client is what's active); MIG-GAP-DOC-001 (migrating backend-hr's own upload stub to call into this module) not done — backend-hr/routes.ts:44-51 still hosts its own separate, still-NotImplementedError stub.
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-DOCUMENTS-001@0.1.4 (FROZEN)  # docs/03-modules/documents/MODULE_SPEC.md. FROZEN at G2 on 2026-07-27 by the commissioning human, per GD-16 (option (a): self-upload + manager-upload only, hotel-scoped read via checkHotelAccess()/checkWorkerScope(), presigned-URL retrieval, SSE-at-rest, malware-scan hook deferred). All five G4 dimensions PASS_WITH_ACTIONS, zero Critical/High. See SYNC_STATE.yaml SYNC-055; DECISION_INDEX GD-16.
    evidence: backend/src/modules/documents/service.ts; backend/src/modules/documents/controller.ts; backend/src/modules/documents/routes.ts; backend/src/routes/v1/index.ts:19,38; backend/prisma/schema.prisma:890 (model WorkerDocument); backend/prisma/migrations/20260727060000_add_worker_document/; backend/src/__tests__/documents-authz.test.ts; backend/src/__tests__/documents-service.test.ts

  - id: backend-chatbot
    type: backend-module
    path: backend/src/modules/chatbot
    owner: unassigned
    lifecycle: declared
    implementation_status: unimplemented-stub  # directory holds only .placeholder; not route-registered
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-CHATBOT-001@0.1.3 (REVIEW)  # G4 round complete (architecture/dependency/consistency/performance/security all PASS_WITH_ACTIONS, zero Critical/High); G2 freeze remains reserved human authority, blocked on OD-CHAT-005/006/013 (SYNC-029)
    evidence: backend/src/modules/chatbot/.placeholder

  - id: backend-geo
    type: backend-module
    path: backend/src/modules/geo
    owner: unassigned
    lifecycle: declared
    implementation_status: unimplemented-stub  # directory holds only .placeholder; not route-registered
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: SPEC-GEO-001@0.1.1 (REVIEW)  # G4 round complete (architecture/dependency/performance PASS, consistency PASS, security PASS_WITH_ACTIONS); G2 freeze reserved human authority, blocked on OD-GEO-001/002 (ownership split) and owner assignment (SYNC-001)
    evidence: backend/src/modules/geo/.placeholder

  - id: frontend-web
    type: web-client
    path: frontend
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: frontend/package.json; ecosystem.config.js:25 (hotel-crm-web); nginx/hotelcrm.conf:92 (proxy_pass to :3000)

  - id: mobile-worker
    type: mobile-client
    path: mobile/worker-app
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: mobile/worker-app/package.json; package.json:9 (mobile/* workspace)

  - id: mobile-checker
    type: mobile-client
    path: mobile/checker-app
    owner: unassigned
    lifecycle: active
    implementation_status: active
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: mobile/checker-app/package.json; package.json:9 (mobile/* workspace)

  - id: operations
    type: infrastructure
    path: "."
    owner: unassigned
    lifecycle: active
    implementation_status: not-applicable  # infrastructure surface, not a backend service module
    dependencies: []
    published_events: none-observed
    consumed_events: none-observed
    specification: UNKNOWN
    evidence: docker-compose.yml; docker-compose.prod.yml; ecosystem.config.js; nginx/hotelcrm.conf; deploy/aws-edge-checklist.md; scripts/deploy.sh

ownership_conflicts: []

unresolved:
  - All accountable owners require authoritative human assignment (SYNC-001, blocked); no CODEOWNERS exists and backend/package.json "author" is empty.
  - backend-chatbot and backend-geo are declared-but-unimplemented stubs (directories contain only .placeholder; not route-registered).
  - backend-hr and backend-calendar are active but lack a dedicated test file in backend/src/__tests__/.
  - SUPERSEDED (2026-07-20, Repository Synchronization Workflow): the prior claim here ("no frozen module specification exists yet") is stale -- ten module specifications are now FROZEN (see the file header above and each module's own `specification` field); only backend-chatbot/backend-geo remain REVIEW-status, both unimplemented stubs. Retained as historical trail, not current state.
  - backend-analytics's `GET /analytics/leaderboard`(`/by-hotel/:hotel_id`) Critical authz gap (OQ-ANALYTICS-01 / SIR-ANLY-001) is now RESOLVED in code by Sprint 0 item S0-5 (EPIC-SECREM, 2026-07-17): both routes enforce `requireRole(['admin','manager'])`, and `/by-hotel/:hotel_id` additionally enforces `checkHotelAccess()`, matching the module's own `/stats`/`/hotel-summary` and the sibling `/quality/leaderboard`. Independent security review PASS (Critical closed); backend suite green (149/149). The Critical no longer blocks SPEC-ANALYTICS-001's G2; the residual manager cross-tenant visibility on `/by-hotel` and the un-gated `getHotelSummary.top_workers` slice are tracked non-blocking under SIR-ANLY-014 (downstream of SIR-AUTH-003).
  - `edge-analytics-reads-hotel-worker` (dependency review, SPEC-ANALYTICS-001 v0.1.1 FIND-DEP-001) is now APPLIED to DEPENDENCY_GRAPH.yaml by the repository-synchronization pass (Package A step 5, AUDIT-M3); `state-hotel-worker.readers` now includes `backend-analytics`.
  - `backend-consent` (target `SPEC-CONSENT-001@0.1.1`, status `REVIEW`) has zero code footprint — no `backend/src/modules/consent` directory exists at all (not even a `.placeholder`, unlike `backend-chatbot`/`backend-geo`) — so no module entry is added here, mirroring the `backend-documents`/`backend-payslips` zero-code deferral pattern. `docs/03-modules/consent/MODULE_SPEC.md` now exists (authored under `ADR-015`, 2026-07-13; completed its first G4 Independent Review round — Architecture/Dependency/Consistency/Security/Performance all `PASS_WITH_ACTIONS`, zero Critical/High findings remaining after v0.1.0→v0.1.1 correction — and G6 documentation validation, `PASS_WITH_ACTIONS`; see `.claude/governance/SPECIFICATION_ISSUES_REGISTER.md`'s `## Module: Consent` section, `SIR-CONSENT-001..011`). `ADR-015` (Accepted, 2026-07-13) pre-emptively settles the ownership boundary: `backend-consent` is the exclusive owner of consent lifecycle/records/versions/withdrawal/renewal/audit history/validation, covering the recurring GDPR access gate (CRR §24) five specs already anticipate plus the one-time chatbot-consent question in `docs/03-modules/onboarding/MODULE_SPEC.md` `OPQ-3` (resolved by that ADR). Not frozen (G2 human) — owner assignment and all 11 open decisions remain standing human-authority items. See `BOUNDARY_INDEX.yaml`'s `backend-consent` row and `DECISION_INDEX.md`.
  - `backend-compliance` (target `SPEC-COMPLIANCE-001@0.1.0`, status `REVIEW`) has zero code footprint — no `backend/src/modules/compliance` directory exists at all — so no module entry is added here, mirroring the `backend-consent`/`backend-documents`/`backend-payslips` zero-code deferral pattern. `docs/03-modules/compliance/MODULE_SPEC.md` now exists (authored 2026-07-14). `ADR-016` (Accepted, 2026-07-14) resolved the pre-authoring G1.5 Boundary Collision Gate FAIL against `state-audit-log`: `backend-auth` is the authoritative writer; `backend-compliance` is a read-only consumer, never a writer or lifecycle owner, of `AuditLog` (backend-auth), Consent (backend-consent, per ADR-015), Retention's deletion-audit log (backend-retention), and Documents' worker-scoped retrievability (backend-documents). Not frozen (G2 human) — owner assignment (SYNC-001) and OD-COMPLIANCE-002..012 remain standing human-authority items. See `BOUNDARY_INDEX.yaml`'s `backend-compliance` row.
  - `backend-retention` (target `SPEC-RETENTION-001@0.2.0`, status `REVIEW`) has zero code footprint — no `backend/src/modules/retention` directory exists at all — so no module entry is added here, mirroring the `backend-consent`/`backend-compliance` zero-code deferral pattern. `docs/03-modules/retention/MODULE_SPEC.md` now exists (authored 2026-07-14), owning the three-tier GDPR retention-classification model, `RetentionLog`, the daily automatic-deletion sweep job, and `IF-RETENTION-*` interfaces. G1.5 PASS at authoring (no other module claimed retention-execution ownership). Not frozen (G2 human) — owner assignment and OD-RETENTION-01/05/10/14 remain standing human-authority items. See `BOUNDARY_INDEX.yaml`'s `backend-retention` row.
  - "RESOLVED by SYNC-055 (2026-07-27, PR #245): the `backend-documents` (target `SPEC-DOCUMENTS-001@0.1.4`, status **FROZEN 2026-07-27 per `GD-16`**) zero-code deferral previously recorded on this line no longer applies. `backend/src/modules/documents` now exists and a module entry has been added above (`implementation_status: active-partial`) — see that entry for the full evidence trail. Retained here, narrowed rather than deleted, per this register's own append/resolve/merge protocol: the earlier sibling notes' \"Documents' worker-scoped retrievability (backend-documents)\" cross-references (in the `backend-compliance` row above) remain accurate as forward-looking design facts and are unaffected by this resolution."
